(function (window, document) {
'use strict';
var STATE = Object.freeze({
LOADING: 'Loading',
LOADED: 'Loaded',
EMPTY: 'Empty',
ERROR: 'Error'
});
var ACTION = Object.freeze({
VIEW: 'View',
CREATE: 'Create',
EDIT: 'Edit',
DELETE: 'Delete',
IMPORT: 'Import',
EXPORT: 'Export',
MANAGE: 'Manage'
});
function clone(value) {
if (value === undefined || value === null) return value;
return JSON.parse(JSON.stringify(value));
}
function toArray(value) {
return Array.isArray(value) ? value : [];
}
function ensureKey(record, keyName) {
if (!record || !record[keyName]) {
throw new Error('Missing required key: ' + keyName);
}
}
function createRegistry(name, keyName) {
var items = new Map();
return {
register: function (record) {
ensureKey(record, keyName);
if (items.has(record[keyName])) {
throw new Error(name + ' already registered: ' + record[keyName]);
}
items.set(record[keyName], Object.freeze(Object.assign({}, record)));
return this;
},
upsert: function (record) {
ensureKey(record, keyName);
items.set(record[keyName], Object.freeze(Object.assign({}, record)));
return this;
},
get: function (key) {
return items.get(key) || null;
},
has: function (key) {
return items.has(key);
},
list: function () {
return Array.from(items.values()).map(clone);
},
clear: function () {
items.clear();
return this;
}
};
}
function pathToRegex(path) {
var keys = [];
var normalized = String(path || '/').replace(/\/+$/, '') || '/';
var source = normalized
.replace(/:([A-Za-z0-9_]+)/g, function (_, key) {
keys.push(key);
return '___PARAM_' + (keys.length - 1) + '___';
})
.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')
.replace(/\//g, '\\/')
.replace(/___PARAM_([0-9]+)___/g, '([^\\/]+)');
return { keys: keys, regex: new RegExp('^' + source + '\\/?$') };
}
function createRouteRegistry() {
var routes = createRegistry('route', 'path');
function enrich(record) {
var compiled = pathToRegex(record.path);
return Object.assign({}, record, {
keys: compiled.keys,
pattern: compiled.regex.source
});
}
return Object.assign({}, routes, {
register: function (record) {
routes.register(enrich(record));
return this;
},
upsert: function (record) {
routes.upsert(enrich(record));
return this;
},
match: function (path) {
var target = String(path || '/').split('?')[0].replace(/\/+$/, '') || '/';
var found = null;
routes.list().some(function (route) {
var regex = new RegExp(route.pattern);
var matched = regex.exec(target);
if (!matched) return false;
var params = {};
toArray(route.keys).forEach(function (key, index) {
params[key] = decodeURIComponent(matched[index + 1] || '');
});
found = Object.assign({}, route, { params: params });
return true;
});
return found;
}
});
}
function normalizeApiResponse(payload, fallbackMessage) {
function normalizeListData(data) {
if (!data || typeof data !== 'object' || Array.isArray(data)) return data;
var hasRows = Array.isArray(data.data) || Array.isArray(data.items);
var hasMeta = data.page !== undefined || data.pageSize !== undefined || data.total !== undefined || data.totalItems !== undefined || data.totalPages !== undefined;
if (!hasRows && !hasMeta) return data;
var rows = Array.isArray(data.data) ? data.data : (Array.isArray(data.items) ? data.items : []);
var page = Math.max(1, Number(data.page || 1));
var pageSize = Math.max(1, Number(data.pageSize || rows.length || 20));
var totalItems = Math.max(0, Number(data.totalItems != null ? data.totalItems : (data.total != null ? data.total : rows.length)));
var totalPages = Math.max(1, Number(data.totalPages || Math.ceil(totalItems / pageSize) || 1));
return Object.assign({}, data, {
data: rows,
items: rows,
page: page,
pageSize: pageSize,
totalItems: totalItems,
total: totalItems,
totalPages: totalPages,
hasNext: data.hasNext !== undefined ? Boolean(data.hasNext) : page < totalPages,
hasPrevious: data.hasPrevious !== undefined ? Boolean(data.hasPrevious) : page > 1
});
}
if (payload && typeof payload === 'object' && payload.success !== undefined && payload.data !== undefined) {
var successData = normalizeListData(payload.data);
return {
success: Boolean(payload.success),
message: payload.message || fallbackMessage || '',
data: successData,
meta: payload.meta || payload.pagination || null,
raw: payload
};
}
if (payload && typeof payload === 'object' && payload.ok !== undefined) {
var okData = normalizeListData(payload.data !== undefined ? payload.data : null);
return {
success: Boolean(payload.ok),
message: payload.message || (payload.error && payload.error.message) || fallbackMessage || '',
data: okData,
meta: payload.meta || payload.pagination || null,
raw: payload
};
}
return {
success: true,
message: fallbackMessage || '',
data: payload,
meta: null,
raw: payload
};
}
function createApiClient(options) {
var config = Object.assign({ fetch: window.fetch && window.fetch.bind(window), baseUrl: '' }, options || {});
function jsonOptions(method, body, options) {
var requestOptions = Object.assign({
method: method,
headers: { 'Content-Type': 'application/json' }
}, options || {});
requestOptions.headers = Object.assign({ 'Content-Type': 'application/json' }, options && options.headers || {});
if (body !== undefined) requestOptions.body = JSON.stringify(body || {});
return requestOptions;
}
function request(path, requestOptions) {
if (typeof config.fetch !== 'function') {
return Promise.reject(new Error('Fetch is not available'));
}
var url = config.baseUrl + path;
return config.fetch(url, requestOptions || {})
.then(function (response) {
if (!response || typeof response.json !== 'function') return response;
return response.json().then(function (body) {
var normalized = normalizeApiResponse(body);
normalized.status = response.status;
normalized.httpOk = response.ok;
return normalized;
});
});
}
return {
normalize: normalizeApiResponse,
request: request,
get: function (path, options) {
return request(path, Object.assign({}, options || {}, { method: 'GET' }));
},
post: function (path, body, options) {
return request(path, jsonOptions('POST', body, options));
},
put: function (path, body, options) {
return request(path, jsonOptions('PUT', body, options));
},
patch: function (path, body, options) {
return request(path, jsonOptions('PATCH', body, options));
},
delete: function (path, options) {
return request(path, Object.assign({ method: 'DELETE' }, options || {}));
},
del: function (path, options) {
return request(path, Object.assign({ method: 'DELETE' }, options || {}));
}
};
}
function createApiResourceService(apiClient, routerService, crudService) {
var methodMap = {
list: 'GET',
detail: 'GET',
create: 'POST',
edit: 'PUT',
update: 'PUT',
delete: 'DELETE'
};
function normalizeEndpointAction(action) {
if (action === 'edit') return 'update';
return action || 'list';
}
function methodFor(action) {
return methodMap[action] || methodMap[normalizeEndpointAction(action)] || 'GET';
}
function encodeQuery(query) {
var parts = [];
Object.keys(query || {}).forEach(function (key) {
var value = query[key];
if (value === undefined || value === null || value === '') return;
if (Array.isArray(value)) {
value.forEach(function (item) {
if (item !== undefined && item !== null && item !== '') parts.push(encodeURIComponent(key) + '=' + encodeURIComponent(item));
});
return;
}
parts.push(encodeURIComponent(key) + '=' + encodeURIComponent(value));
});
return parts.length ? '?' + parts.join('&') : '';
}
function endpoint(moduleKey, operation, options) {
var config = options || {};
var action = operation || 'list';
var params = config.params || {};
var query = config.query || {};
var route = routerService.pathFor(moduleKey, action === 'update' ? 'edit' : action, params);
if (action === 'delete' && params.id) route = routerService.pathFor(moduleKey, 'detail', params);
return route + encodeQuery(query);
}
function operation(moduleKey, action, options) {
var config = options || {};
var workflow = crudService.operationFor(moduleKey, action === 'update' ? 'edit' : action, config.permissionContext || {});
return Object.assign({}, workflow || {}, {
endpoint: endpoint(moduleKey, action, config)
});
}
function inspect(moduleKey, action, options) {
var config = options || {};
var targetAction = action || 'list';
var endpointAction = normalizeEndpointAction(targetAction);
var info = operation(moduleKey, endpointAction, config);
return Object.assign({}, info, {
moduleKey: moduleKey,
action: targetAction,
operation: endpointAction,
method: methodFor(targetAction),
endpoint: endpoint(moduleKey, endpointAction, config)
});
}
return {
methodFor: methodFor,
endpoint: endpoint,
operation: operation,
inspect: inspect,
list: function (moduleKey, query, options) {
return apiClient.get(endpoint(moduleKey, 'list', Object.assign({}, options || {}, { query: query || {} })), options || {});
},
detail: function (moduleKey, id, options) {
return apiClient.get(endpoint(moduleKey, 'detail', Object.assign({}, options || {}, { params: { id: id } })), options || {});
},
create: function (moduleKey, body, options) {
return apiClient.post(endpoint(moduleKey, 'create', options || {}), body || {}, options || {});
},
update: function (moduleKey, id, body, options) {
return apiClient.put(endpoint(moduleKey, 'update', Object.assign({}, options || {}, { params: { id: id } })), body || {}, options || {});
},
delete: function (moduleKey, id, options) {
return apiClient.delete(endpoint(moduleKey, 'delete', Object.assign({}, options || {}, { params: { id: id } })), options || {});
},
del: function (moduleKey, id, options) {
return this.delete(moduleKey, id, options || {});
}
};
}
function createCrudDataService(apiResourceService, stateService) {
function inspect(moduleKey, action, options) {
var operation = action || 'list';
var info = apiResourceService.inspect ? apiResourceService.inspect(moduleKey, operation, options || {}) : {
moduleKey: moduleKey,
action: operation,
operation: operation
};
return Object.assign({}, info, {
stateStatus: stateService.statusFor(moduleKey),
state: stateService.get(moduleKey)
});
}
function settle(moduleKey, request, options) {
var config = options || {};
stateService.loading(moduleKey, { operation: config.operation || null });
function accept(response) {
var data = response && response.data !== undefined ? response.data : response;
if (Array.isArray(data) && !data.length) {
stateService.empty(moduleKey, { operation: config.operation || null, response: response || null });
} else {
stateService.loaded(moduleKey, data || null, { operation: config.operation || null, response: response || null });
}
return response;
}
function reject(error) {
stateService.error(moduleKey, error && error.message || String(error), { operation: config.operation || null });
throw error;
}
try {
var result = typeof request === 'function' ? request() : request;
if (result && typeof result.then === 'function') return result.then(accept).catch(reject);
return accept(result);
} catch (error) {
return reject(error);
}
}
function list(moduleKey, query, options) {
return settle(moduleKey, function () {
return apiResourceService.list(moduleKey, query || {}, options || {});
}, { operation: 'list' });
}
function detail(moduleKey, id, options) {
return settle(moduleKey, function () {
return apiResourceService.detail(moduleKey, id, options || {});
}, { operation: 'detail' });
}
function create(moduleKey, body, options) {
return settle(moduleKey, function () {
return apiResourceService.create(moduleKey, body || {}, options || {});
}, { operation: 'create' });
}
function update(moduleKey, id, body, options) {
return settle(moduleKey, function () {
return apiResourceService.update(moduleKey, id, body || {}, options || {});
}, { operation: 'update' });
}
function remove(moduleKey, id, options) {
return settle(moduleKey, function () {
return apiResourceService.delete(moduleKey, id, options || {});
}, { operation: 'delete' });
}
return {
inspect: inspect,
list: list,
detail: detail,
create: create,
update: update,
delete: remove,
del: remove
};
}
function createModuleLoaderService(moduleRegistry, stateService, permissionService) {
function resolveModule(target) {
if (!target) return null;
if (typeof target === 'string') return moduleRegistry.get(target);
if (target.moduleKey) return moduleRegistry.get(target.moduleKey);
if (target.screenId) {
return moduleRegistry.list().find(function (module) {
return module.screenId === target.screenId;
}) || null;
}
return null;
}
function loaderFor(moduleKey, options) {
var module = resolveModule(moduleKey);
var config = options || {};
if (!module || !module.loaderName) return null;
if (config.loaders && typeof config.loaders[module.loaderName] === 'function') return config.loaders[module.loaderName];
return typeof window[module.loaderName] === 'function' ? window[module.loaderName] : null;
}
function inspect(target, options) {
var module = resolveModule(target);
var loader = module ? loaderFor(module.moduleKey, options || {}) : null;
var loaderName = module && module.loaderName || null;
return {
registered: !!module,
moduleKey: module ? module.moduleKey : null,
screenId: module ? module.screenId : null,
route: module ? module.path || null : null,
loaderName: loaderName,
available: !!loader,
stateStatus: module ? stateService.statusFor(module.moduleKey) : null,
missingReason: module ? (loaderName ? (loader ? null : 'loaderUnavailable') : 'loaderNotConfigured') : 'moduleNotRegistered'
};
}
function load(target, context) {
var module = resolveModule(target);
var config = context || {};
if (!module) throw new Error('Module not registered: ' + (target && target.moduleKey || target));
var scope = module.permissionScope || module.moduleKey;
var allowed = typeof window.thon09CanAccess === 'function'
? window.thon09CanAccess(scope, 'read')
: (!permissionService || permissionService.can(scope, 'read', window.App && window.App.user));
if (!allowed) {
var screen = document.getElementById((module.screenId || module.moduleKey) + 'Screen');
if (screen && !screen.dataset.accessDeniedRendered) {
screen.dataset.accessDeniedRendered = '1';
var targetNode = screen.querySelector('tbody, [id$="Rows"], [id$="Kpis"], .content-card, .dashboard-kpi-grid') || screen;
if (targetNode.tagName === 'TBODY') targetNode.innerHTML = '<tr><td colspan="12" class="text-center text-muted py-4">Bạn không có quyền truy cập</td></tr>';
else targetNode.innerHTML = '<div class="empty-state text-muted py-4 text-center">Bạn không có quyền truy cập</div>';
}
return {
moduleKey: module.moduleKey,
loaderName: module.loaderName || null,
loaded: false,
skipped: true,
denied: true
};
}
var loader = loaderFor(module.moduleKey, config);
if (!loader) {
return {
moduleKey: module.moduleKey,
loaderName: module.loaderName || null,
loaded: false,
skipped: true
};
}
stateService.loading(module.moduleKey, { loaderName: module.loaderName });
try {
var result = loader({
module: clone(module),
moduleKey: module.moduleKey,
screenId: module.screenId,
route: config.route || module.path || null,
params: clone(config.params || {})
});
if (result && typeof result.then === 'function') {
return result.then(function (value) {
stateService.loaded(module.moduleKey, value || null, { loaderName: module.loaderName });
return { moduleKey: module.moduleKey, loaderName: module.loaderName, loaded: true, result: value };
}).catch(function (error) {
stateService.error(module.moduleKey, error && error.message || String(error), { loaderName: module.loaderName });
throw error;
});
}
stateService.loaded(module.moduleKey, result || null, { loaderName: module.loaderName });
return { moduleKey: module.moduleKey, loaderName: module.loaderName, loaded: true, result: result };
} catch (error) {
stateService.error(module.moduleKey, error && error.message || String(error), { loaderName: module.loaderName });
throw error;
}
}
function preload(keys, context) {
return toArray(keys).map(function (moduleKey) {
return load(moduleKey, context || {});
});
}
return {
resolveModule: resolveModule,
loaderFor: loaderFor,
inspect: inspect,
load: load,
preload: preload
};
}
function createPermissionService() {
var rules = new Map();
var moduleAliases = {
household: 'households',
households: 'households',
citizen: 'persons',
citizens: 'persons',
person: 'persons',
persons: 'persons',
movement: 'movements',
movements: 'movements',
temporary_residence: 'temporaryResidence',
temporaryResidence: 'temporaryResidence',
temporary_absence: 'temporaryAbsence',
temporaryAbsence: 'temporaryAbsence',
public_asset: 'publicAssets',
public_assets: 'publicAssets',
publicAssets: 'publicAssets',
household_business: 'businessHouseholds',
household_businesses: 'businessHouseholds',
businessHouseholds: 'businessHouseholds',
house: 'houses',
houses: 'houses',
vehicle: 'vehicles',
vehicles: 'vehicles',
dashboard: 'dashboard',
dashboardHouseholds: 'dashboard',
dashboardPopulation: 'dashboard',
dashboardBusiness: 'dashboard',
dashboardVehicles: 'dashboard',
dashboardLivestock: 'dashboard',
dashboardGis: 'dashboard',
dashboardReports: 'dashboard',
operationCenter: 'dashboard',
operation_center: 'dashboard',
reports: 'report',
report: 'report',
users: 'user',
user: 'user',
permissions: 'permission',
permission: 'permission',
logs: 'logs',
log: 'logs',
settings: 'settings',
appearance: 'settings',
backups: 'backup',
backup: 'backup',
restore: 'backup',
systemAdmin: 'system_admin',
system_admin: 'system_admin',
import: 'import',
export: 'report',
contributions: 'contributions',
agriculture: 'agriculture',
livestock: 'livestock',
gis: 'gis'
};
var actionAliases = {
view: ACTION.VIEW,
read: ACTION.VIEW,
list: ACTION.VIEW,
create: ACTION.CREATE,
add: ACTION.CREATE,
edit: ACTION.EDIT,
update: ACTION.EDIT,
delete: ACTION.DELETE,
remove: ACTION.DELETE,
import: ACTION.IMPORT,
export: ACTION.EXPORT,
manage: ACTION.MANAGE,
admin: ACTION.MANAGE
};
function key(moduleKey, action) {
return normalizeModule(moduleKey || '*') + ':' + normalizeAction(action || ACTION.VIEW);
}
function normalizeModule(moduleKey) {
var value = String(moduleKey || '*');
return moduleAliases[value] || moduleAliases[value.replace(/-/g, '_')] || value;
}
function normalizeAction(action) {
var value = String(action || ACTION.VIEW);
return actionAliases[value] || actionAliases[value.toLowerCase()] || value;
}
function set(moduleKey, action, allowed) {
rules.set(key(moduleKey, action), Boolean(allowed));
return api;
}
function setMany(permissions) {
Object.keys(permissions || {}).forEach(function (moduleKey) {
var moduleRules = permissions[moduleKey] || {};
Object.keys(moduleRules).forEach(function (action) {
set(moduleKey, action, moduleRules[action]);
});
});
return api;
}
function loadUser(user) {
if (user && user.permissions) setMany(user.permissions);
return api;
}
function loadMatrix(matrix) {
toArray(matrix && matrix.roles).forEach(function (role) {
if (role && role.permissions) setMany(role.permissions);
});
if (matrix && matrix.permissions) setMany(matrix.permissions);
return api;
}
function can(moduleKey, action, user) {
var exact = key(moduleKey, action);
var wildcard = key(moduleKey, '*');
var manage = key(moduleKey, ACTION.MANAGE);
if (rules.has(exact)) return rules.get(exact);
if (rules.has(manage)) return rules.get(manage);
if (rules.has(wildcard)) return rules.get(wildcard);
if (user && String(user.role || '').toUpperCase() === 'SUPER_ADMIN') return true;
return false;
}
var api = {
normalizeModule: normalizeModule,
normalizeAction: normalizeAction,
set: set,
setMany: setMany,
loadUser: loadUser,
loadMatrix: loadMatrix,
can: can,
canAll: function (checks, user) {
return toArray(checks).every(function (check) {
return can(check.moduleKey || check.module || check.scope, check.action, user);
});
},
canAny: function (checks, user) {
return toArray(checks).some(function (check) {
return can(check.moduleKey || check.module || check.scope, check.action, user);
});
},
list: function () {
return Array.from(rules.entries()).map(function (entry) {
return { key: entry[0], allowed: entry[1] };
});
},
clear: function () {
rules.clear();
return this;
}
};
return api;
}
function createPermissionViewService(permissionService, componentService) {
function setAttr(node, name, value) {
if (!node) return;
if (node.setAttribute) node.setAttribute(name, value);
else {
node.attributes = node.attributes || {};
node.attributes[name] = value;
}
}
function removeAttr(node, name) {
if (!node) return;
if (node.removeAttribute) node.removeAttribute(name);
else if (node.attributes) delete node.attributes[name];
}
function state(moduleKey, action, context) {
var allowed = permissionService.can(moduleKey, action, context || {});
return {
moduleKey: permissionService.normalizeModule(moduleKey),
action: permissionService.normalizeAction(action),
allowed: allowed,
hidden: !allowed,
disabled: !allowed
};
}
function attrs(moduleKey, action, context) {
var info = state(moduleKey, action, context || {});
return info.allowed ? {} : { hidden: 'hidden', disabled: 'disabled', 'aria-disabled': 'true' };
}
function apply(node, moduleKey, action, context, options) {
if (!node) return null;
var config = options || {};
var info = state(moduleKey, action, context || {});
node.dataset = node.dataset || {};
node.dataset.permissionModule = info.moduleKey;
node.dataset.permissionAction = info.action;
node.dataset.permissionAllowed = String(info.allowed);
if (info.allowed) {
removeAttr(node, 'hidden');
removeAttr(node, 'disabled');
removeAttr(node, 'aria-disabled');
node.hidden = false;
node.disabled = false;
} else {
if (config.hide !== false) {
setAttr(node, 'hidden', 'hidden');
node.hidden = true;
}
if (config.disable !== false) {
setAttr(node, 'disabled', 'disabled');
node.disabled = true;
}
setAttr(node, 'aria-disabled', 'true');
}
return info;
}
function button(config) {
var options = config || {};
var info = state(options.moduleKey, options.permissionAction || options.action || ACTION.VIEW, options.permissionContext || {});
var attrsConfig = Object.assign({}, options.attrs || {}, info.allowed ? {} : { disabled: 'disabled', 'aria-disabled': 'true' });
var dataset = Object.assign({}, options.dataset || {}, {
permissionModule: info.moduleKey,
permissionAction: info.action,
permissionAllowed: String(info.allowed)
});
return componentService.button(Object.assign({}, options, {
attrs: attrsConfig,
dataset: dataset
}));
}
function filterActions(moduleKey, actions, context) {
return toArray(actions).filter(function (action) {
var key = action && (action.permissionAction || action.action || action.key) || action;
return permissionService.can(moduleKey, key, context || {});
});
}
return {
state: state,
attrs: attrs,
apply: apply,
button: button,
filterActions: filterActions
};
}
function createStateService() {
var records = new Map();
var subscribers = [];
var allowedStatuses = Object.keys(STATE).map(function (key) {
return STATE[key];
});
function normalize(moduleKey, status, options) {
if (!moduleKey) throw new Error('Module key is required');
if (allowedStatuses.indexOf(status) === -1) {
throw new Error('Invalid module state: ' + status);
}
var config = options || {};
return {
moduleKey: moduleKey,
status: status,
data: config.data === undefined ? null : config.data,
error: config.error || null,
meta: config.meta || null,
updatedAt: Date.now()
};
}
function emit(record) {
var snapshot = clone(record);
if (document && document.dispatchEvent) {
document.dispatchEvent(new CustomEvent('thon09:module-state-change', { detail: snapshot }));
}
subscribers.slice().forEach(function (subscriber) {
if (!subscriber.moduleKey || subscriber.moduleKey === record.moduleKey) {
subscriber.callback(clone(record));
}
});
return snapshot;
}
function set(moduleKey, status, options) {
var record = normalize(moduleKey, status, options || {});
records.set(moduleKey, record);
return emit(record);
}
function subscribe(callback, options) {
if (typeof callback !== 'function') throw new Error('State subscriber callback is required');
var subscriber = {
callback: callback,
moduleKey: options && options.moduleKey || null
};
subscribers.push(subscriber);
return function unsubscribe() {
var index = subscribers.indexOf(subscriber);
if (index !== -1) subscribers.splice(index, 1);
return index !== -1;
};
}
return {
set: set,
subscribe: subscribe,
onChange: subscribe,
loading: function (moduleKey, meta) {
return set(moduleKey, STATE.LOADING, { meta: meta || null });
},
loaded: function (moduleKey, data, meta) {
return set(moduleKey, STATE.LOADED, { data: data === undefined ? null : data, meta: meta || null });
},
empty: function (moduleKey, meta) {
return set(moduleKey, STATE.EMPTY, { meta: meta || null });
},
error: function (moduleKey, error, meta) {
return set(moduleKey, STATE.ERROR, { error: error || null, meta: meta || null });
},
get: function (moduleKey) {
return records.has(moduleKey) ? clone(records.get(moduleKey)) : null;
},
statusFor: function (moduleKey) {
return records.has(moduleKey) ? records.get(moduleKey).status : STATE.EMPTY;
},
is: function (moduleKey, status) {
return (records.has(moduleKey) ? records.get(moduleKey).status : STATE.EMPTY) === status;
},
list: function () {
return Array.from(records.values()).map(clone);
},
summary: function () {
var result = { total: records.size };
allowedStatuses.forEach(function (status) {
result[status] = 0;
});
records.forEach(function (record) {
result[record.status] += 1;
});
return result;
},
clear: function (moduleKey) {
if (moduleKey) records.delete(moduleKey);
else records.clear();
return this;
},
subscriberCount: function () {
return subscribers.length;
}
};
}
function createActionService() {
var registry = createRegistry('action', 'key');
var boundRoots = [];
var defaultSelector = '[data-platform-action]';
function normalize(recordOrKey, handler) {
if (typeof recordOrKey === 'string') {
return { key: recordOrKey, handler: handler };
}
return recordOrKey;
}
function register(recordOrKey, handler) {
var record = normalize(recordOrKey, handler);
if (!record || typeof record.handler !== 'function') {
throw new Error('Action handler is required');
}
registry.upsert(record);
return this;
}
function dispatch(key, context) {
var action = registry.get(key);
if (!action) throw new Error('Action not registered: ' + key);
return action.handler(Object.assign({ key: key }, context || {}));
}
function targetFor(eventOrTarget) {
var seed = eventOrTarget && eventOrTarget.target ? eventOrTarget.target : eventOrTarget;
if (!seed) return null;
if (seed.closest) return seed.closest(defaultSelector);
if (seed.dataset && seed.dataset.platformAction) return seed;
return null;
}
function contextFor(eventOrTarget, extra) {
var event = eventOrTarget && eventOrTarget.target ? eventOrTarget : null;
var target = targetFor(eventOrTarget);
if (!target || !target.dataset || !target.dataset.platformAction) return null;
return Object.assign({
event: event,
target: target,
key: target.dataset.platformAction,
dataset: Object.assign({}, target.dataset || {})
}, extra || {});
}
function handleClick(event) {
var context = contextFor(event);
if (!context) return null;
if (event.preventDefault) event.preventDefault();
return dispatch(context.key, context);
}
function bind(root) {
var host = root || document;
if (!host || typeof host.addEventListener !== 'function' || boundRoots.indexOf(host) !== -1) return false;
host.addEventListener('click', handleClick);
boundRoots.push(host);
return true;
}
function unbind(root) {
var host = root || document;
var index = boundRoots.indexOf(host);
if (index === -1 || !host || typeof host.removeEventListener !== 'function') return false;
host.removeEventListener('click', handleClick);
boundRoots.splice(index, 1);
return true;
}
return {
register: register,
upsert: register,
get: registry.get,
list: registry.list,
dispatch: dispatch,
targetFor: targetFor,
contextFor: contextFor,
bind: bind,
unbind: unbind,
isBound: function (root) {
return boundRoots.indexOf(root || document) !== -1;
},
boundCount: function () {
return boundRoots.length;
},
handleClick: handleClick,
selector: defaultSelector
};
}
function createComponentService(actionService, stateService) {
function append(parent, child) {
if (child === undefined || child === null) return;
if (Array.isArray(child)) {
child.forEach(function (item) {
append(parent, item);
});
return;
}
if (typeof child === 'string' || typeof child === 'number') {
parent.appendChild(document.createTextNode(String(child)));
return;
}
parent.appendChild(child);
}
function element(tag, options, children) {
if (!document || typeof document.createElement !== 'function') {
throw new Error('Document is not available');
}
var config = options || {};
var node = document.createElement(tag);
if (config.className) node.className = config.className;
if (config.text !== undefined) node.textContent = String(config.text);
if (config.html !== undefined) node.innerHTML = String(config.html);
Object.keys(config.attrs || {}).forEach(function (name) {
node.setAttribute(name, config.attrs[name]);
});
Object.keys(config.dataset || {}).forEach(function (name) {
node.dataset[name] = config.dataset[name];
});
Object.keys(config.on || {}).forEach(function (name) {
node.addEventListener(name, config.on[name]);
});
append(node, children || []);
return node;
}
function button(config) {
var options = config || {};
var dataset = Object.assign({}, options.dataset || {});
if (options.action) dataset.platformAction = options.action;
var children = [];
if (options.icon) children.push(element('i', { className: 'fa-solid ' + options.icon, attrs: { 'aria-hidden': 'true' } }));
if (options.label !== undefined) children.push(element('span', { text: options.label }));
return element('button', {
className: options.className || ('btn btn-' + (options.variant || 'primary')),
attrs: Object.assign({ type: options.type || 'button' }, options.attrs || {}),
dataset: dataset,
on: options.on || {}
}, children);
}
function badge(config) {
var options = config || {};
return element('span', {
className: options.className || ('badge text-bg-' + (options.tone || 'secondary')),
text: options.label || ''
});
}
function status(config) {
var options = config || {};
var key = options.key || options.status || 'unknown';
var tones = Object.assign({
active: 'success',
inactive: 'secondary',
pending: 'warning',
error: 'danger',
deleted: 'danger',
archived: 'secondary'
}, options.tones || {});
var labels = Object.assign({
active: 'Dang hoat dong',
inactive: 'Tam dung',
pending: 'Cho xu ly',
error: 'Loi',
deleted: 'Da xoa',
archived: 'Luu tru'
}, options.labels || {});
return element('span', {
className: options.className || ('platform-status badge text-bg-' + (tones[key] || options.tone || 'secondary')),
text: options.label || labels[key] || String(key),
dataset: Object.assign({ statusKey: key }, options.dataset || {})
});
}
function card(config, children) {
var options = config || {};
var body = element('div', { className: options.bodyClassName || 'card-body' }, children || []);
var parts = [];
if (options.title || options.subtitle) {
var headerChildren = [];
if (options.title) headerChildren.push(element('h3', { className: options.titleClassName || 'card-title', text: options.title }));
if (options.subtitle) headerChildren.push(element('p', { className: options.subtitleClassName || 'card-subtitle', text: options.subtitle }));
parts.push(element('div', { className: options.headerClassName || 'card-header' }, headerChildren));
}
parts.push(body);
if (options.footer) parts.push(element('div', { className: options.footerClassName || 'card-footer' }, options.footer));
return element('section', {
className: options.className || 'card platform-card',
attrs: options.attrs || {},
dataset: options.dataset || {}
}, parts);
}
function form(config, children) {
var options = config || {};
var attrs = Object.assign({
method: options.method || 'post'
}, options.attrs || {});
if (options.id) attrs.id = options.id;
if (options.action) attrs.action = options.action;
if (options.noValidate) attrs.novalidate = 'novalidate';
var dataset = Object.assign({}, options.dataset || {});
if (options.formKey) dataset.formKey = options.formKey;
if (options.moduleKey) dataset.moduleKey = options.moduleKey;
var content = [];
if (options.title || options.subtitle) {
var header = [];
if (options.title) header.push(element('h3', { className: options.titleClassName || 'platform-form-title', text: options.title }));
if (options.subtitle) header.push(element('p', { className: options.subtitleClassName || 'platform-form-subtitle', text: options.subtitle }));
content.push(element('div', { className: options.headerClassName || 'platform-form-header' }, header));
}
if (Array.isArray(children)) {
children.forEach(function (child) {
content.push(child);
});
} else if (children !== undefined && children !== null) {
content.push(children);
}
if (options.footer) {
content.push(element('div', { className: options.footerClassName || 'platform-form-footer' }, options.footer));
}
if (toArray(options.actions).length) {
content.push(element('div', { className: options.actionsClassName || 'platform-form-actions' }, toArray(options.actions).map(function (action) {
if (!action || action.tagName) return action;
return button(action);
})));
}
return element('form', {
className: options.className || 'platform-form',
attrs: attrs,
dataset: dataset,
on: options.on || {}
}, content);
}
function input(config) {
var options = config || {};
var attrs = Object.assign({
type: options.type || 'text',
name: options.name || options.key || '',
value: options.value !== undefined ? options.value : ''
}, options.attrs || {});
if (options.placeholder) attrs.placeholder = options.placeholder;
if (options.disabled) attrs.disabled = 'disabled';
if (options.required) attrs.required = 'required';
return element('input', {
className: options.className || 'form-control',
attrs: attrs,
dataset: options.dataset || {},
on: options.on || {}
});
}
function select(config) {
var options = config || {};
var selectedValue = options.value !== undefined ? String(options.value) : '';
var attrs = Object.assign({
name: options.name || options.key || ''
}, options.attrs || {});
if (options.disabled) attrs.disabled = 'disabled';
if (options.required) attrs.required = 'required';
var choices = toArray(options.options).map(function (choice) {
var value = choice && choice.value !== undefined ? String(choice.value) : String(choice || '');
var label = choice && choice.label !== undefined ? choice.label : value;
var optionAttrs = { value: value };
if (value === selectedValue) optionAttrs.selected = 'selected';
return element('option', { text: label, attrs: optionAttrs });
});
return element('select', {
className: options.className || 'form-select',
attrs: attrs,
dataset: options.dataset || {},
on: options.on || {}
}, choices);
}
function searchBox(config) {
var options = config || {};
return input(Object.assign({
type: 'search',
name: options.name || 'search',
placeholder: options.placeholder || 'Tim kiem',
className: options.className || 'form-control platform-search'
}, options));
}
function filterBar(config) {
var options = config || {};
var controls = [];
if (options.search !== false) {
controls.push(searchBox(options.search && typeof options.search === 'object' ? options.search : {}));
}
toArray(options.filters).forEach(function (filter) {
if (!filter) return;
if (filter.type === 'select') {
controls.push(select({
name: filter.name || filter.key,
value: filter.value !== undefined ? filter.value : filter.defaultValue,
options: filter.options || [],
className: filter.className || 'form-select platform-filter',
dataset: { filterKey: filter.key || filter.name }
}));
return;
}
controls.push(input({
type: filter.type || 'text',
name: filter.name || filter.key,
value: filter.value !== undefined ? filter.value : filter.defaultValue,
placeholder: filter.placeholder || filter.label || '',
className: filter.className || 'form-control platform-filter',
dataset: { filterKey: filter.key || filter.name }
}));
});
return element('div', {
className: options.className || 'platform-filter-bar',
dataset: options.dataset || {}
}, controls);
}
function tabs(config) {
var options = config || {};
var activeKey = options.activeKey || (toArray(options.tabs)[0] && toArray(options.tabs)[0].key);
var nav = element('div', { className: options.navClassName || 'platform-tabs-nav', attrs: { role: 'tablist' } });
var panels = element('div', { className: options.panelsClassName || 'platform-tabs-panels' });
toArray(options.tabs).forEach(function (tab) {
if (!tab || !tab.key) return;
var selected = tab.key === activeKey;
nav.appendChild(button({
label: tab.label || tab.key,
className: tab.buttonClassName || ('platform-tab-button' + (selected ? ' active' : '')),
attrs: {
role: 'tab',
'aria-selected': selected ? 'true' : 'false',
'aria-controls': tab.key + '-panel'
},
dataset: { tabKey: tab.key },
action: options.action
}));
var panelAttrs = {
id: tab.key + '-panel',
role: 'tabpanel'
};
if (!selected) panelAttrs.hidden = 'hidden';
panels.appendChild(element('section', {
className: tab.panelClassName || ('platform-tab-panel' + (selected ? ' active' : '')),
attrs: panelAttrs,
dataset: { tabKey: tab.key }
}, tab.content || []));
});
return element('div', {
className: options.className || 'platform-tabs',
dataset: options.dataset || {}
}, [nav, panels]);
}
function upload(config) {
var options = config || {};
var inputAttrs = Object.assign({
type: 'file',
name: options.name || options.key || 'attachments'
}, options.attrs || {});
if (options.accept) inputAttrs.accept = options.accept;
if (options.multiple !== false) inputAttrs.multiple = 'multiple';
if (options.disabled) inputAttrs.disabled = 'disabled';
var children = [];
if (options.label) {
children.push(element('label', {
className: options.labelClassName || 'form-label',
text: options.label,
attrs: options.id ? { for: options.id } : {}
}));
}
children.push(element('input', {
className: options.inputClassName || 'form-control',
attrs: Object.assign(options.id ? { id: options.id } : {}, inputAttrs),
dataset: options.dataset || {},
on: options.on || {}
}));
if (options.helpText) {
children.push(element('div', { className: options.helpClassName || 'form-text', text: options.helpText }));
}
return element('div', {
className: options.className || 'platform-upload',
attrs: options.wrapperAttrs || {}
}, children);
}
function stateView(status, options) {
var config = options || {};
var error = config.error || null;
var labels = {
Loading: config.loadingText || 'Dang tai...',
Loaded: config.loadedText || '',
Empty: config.emptyText || 'Khong co du lieu',
Error: config.errorText || error || 'Khong the tai du lieu'
};
return element('div', {
className: config.className || ('platform-state platform-state-' + String(status || '').toLowerCase()),
text: labels[status] || '',
dataset: Object.assign({ stateStatus: status || '' }, config.dataset || {})
});
}
function moduleState(moduleKey, options) {
var record = stateService.get(moduleKey);
var config = Object.assign({}, options || {}, {
error: record && record.error || options && options.error || null,
dataset: Object.assign({ moduleKey: moduleKey || '' }, options && options.dataset || {})
});
return stateView(record ? record.status : STATE.EMPTY, config);
}
function table(config) {
var options = config || {};
var columns = toArray(options.columns).filter(function (column) {
return column && column.visible !== false;
});
var rows = toArray(options.rows);
var node = element('table', {
className: options.className || 'table platform-table',
attrs: options.attrs || {}
});
var thead = element('thead');
var headRow = element('tr');
columns.forEach(function (column) {
headRow.appendChild(element('th', {
text: column.label || column.key,
attrs: column.sortable ? { 'data-sortable': 'true', 'data-sort-key': column.key } : {}
}));
});
thead.appendChild(headRow);
node.appendChild(thead);
var tbody = element('tbody');
if (!rows.length) {
var emptyRow = element('tr', { className: 'platform-table-empty-row' });
emptyRow.appendChild(element('td', {
className: 'platform-table-empty',
text: options.emptyText || 'Khong co du lieu',
attrs: { colspan: String(Math.max(columns.length, 1)) }
}));
tbody.appendChild(emptyRow);
} else {
rows.forEach(function (row, rowIndex) {
var tr = element('tr', {
dataset: options.rowKey ? { rowKey: row && row[options.rowKey] } : {}
});
columns.forEach(function (column) {
var value = typeof column.render === 'function'
? column.render(row, rowIndex)
: row && row[column.key];
tr.appendChild(element('td', {}, value === undefined || value === null ? '' : value));
});
tbody.appendChild(tr);
});
}
node.appendChild(tbody);
return node;
}
function pagination(config) {
var options = Object.assign({ page: 1, pageSize: 20, total: 0 }, config || {});
var total = Number(options.totalItems != null ? options.totalItems : options.total || 0);
var totalPages = Math.max(1, Number(options.totalPages || Math.ceil(total / Number(options.pageSize || 20))));
var currentPage = Math.min(Math.max(1, Number(options.page || 1)), totalPages);
var container = element('nav', {
className: options.className || 'platform-pagination',
attrs: { 'aria-label': options.label || 'Phan trang' }
});
var previous = button({
label: options.previousLabel || 'Truoc',
variant: options.variant || 'light',
className: options.buttonClassName || 'btn btn-light',
attrs: currentPage <= 1 ? { disabled: 'disabled' } : {},
dataset: { page: String(Math.max(1, currentPage - 1)) },
action: options.action
});
var status = element('span', {
className: 'platform-pagination-status',
text: 'Trang ' + currentPage + ' / ' + totalPages
});
var next = button({
label: options.nextLabel || 'Sau',
variant: options.variant || 'light',
className: options.buttonClassName || 'btn btn-light',
attrs: currentPage >= totalPages ? { disabled: 'disabled' } : {},
dataset: { page: String(Math.min(totalPages, currentPage + 1)) },
action: options.action
});
append(container, [previous, status, next]);
return container;
}
return {
element: element,
button: button,
badge: badge,
status: status,
card: card,
form: form,
input: input,
select: select,
searchBox: searchBox,
filterBar: filterBar,
tabs: tabs,
upload: upload,
stateView: stateView,
moduleState: moduleState,
table: table,
pagination: pagination,
actionSelector: actionService.selector
};
}
function createFormService() {
var registry = createRegistry('form', 'key');
var sectionOrder = ['basic', 'linked', 'extended', 'attachments'];
function normalize(record) {
ensureKey(record, 'key');
var sections = Object.assign({
basic: [],
linked: [],
extended: [],
attachments: []
}, record.sections || {});
return Object.assign({}, record, {
moduleKey: record.moduleKey || record.key,
modalKey: record.modalKey || null,
fields: toArray(record.fields),
actions: toArray(record.actions),
sections: sections
});
}
function register(record) {
registry.upsert(normalize(record));
return api;
}
function fieldsFor(key, section) {
var form = registry.get(key);
if (!form) return [];
if (section) return toArray(form.sections && form.sections[section]);
var seen = {};
var fields = [];
sectionOrder.forEach(function (sectionKey) {
toArray(form.sections && form.sections[sectionKey]).forEach(function (field) {
var name = field && (field.name || field.key);
if (!name || seen[name]) return;
seen[name] = true;
fields.push(field);
});
});
toArray(form.fields).forEach(function (field) {
var name = field && (field.name || field.key);
if (!name || seen[name]) return;
seen[name] = true;
fields.push(field);
});
return fields;
}
function serialize(form) {
var data = {};
var elements = form && form.elements ? Array.prototype.slice.call(form.elements) : [];
elements.forEach(function (element) {
if (!element || !element.name || element.disabled) return;
if ((element.type === 'checkbox' || element.type === 'radio') && !element.checked) return;
if (element.type === 'file') {
data[element.name] = element.files || [];
return;
}
data[element.name] = element.value;
});
return data;
}
function defaultsFor(key) {
var data = {};
fieldsFor(key).forEach(function (field) {
var name = field && (field.name || field.key);
if (!name) return;
data[name] = field.defaultValue !== undefined ? field.defaultValue : '';
});
return data;
}
var api = {
register: register,
upsert: register,
get: registry.get,
list: registry.list,
fieldsFor: fieldsFor,
serialize: serialize,
defaultsFor: defaultsFor,
sectionOrder: function () {
return sectionOrder.slice();
}
};
return api;
}
function createFormViewService(componentService, formService) {
function field(fieldConfig, values) {
var config = fieldConfig || {};
var payload = values || {};
var fieldKey = config.name || config.key;
var value = payload[fieldKey];
var control;
if (config.type === 'select') {
control = componentService.select(Object.assign({}, config, { value: value !== undefined ? value : config.defaultValue }));
} else if (config.type === 'file' || config.type === 'upload') {
control = componentService.upload(Object.assign({}, config, { label: null }));
} else {
control = componentService.input(Object.assign({}, config, { value: value !== undefined ? value : config.defaultValue }));
}
var children = [];
if (config.label) children.push(componentService.element('label', { className: 'form-label', text: config.label }));
children.push(control);
if (config.helpText) children.push(componentService.element('div', { className: 'form-text', text: config.helpText }));
return componentService.element('div', {
className: config.wrapperClassName || 'platform-form-field',
dataset: { fieldKey: fieldKey || '' }
}, children);
}
function section(formKey, sectionKey, values, options) {
var form = formService.get(formKey);
var config = options || {};
var fields = config.fields || formService.fieldsFor(formKey, sectionKey);
var labels = {
basic: 'Thong tin co ban',
linked: 'Thong tin lien ket',
extended: 'Thong tin mo rong',
history: 'Lich su',
attachments: 'Tep dinh kem'
};
var children = [];
if (config.title !== false) children.push(componentService.element('h3', { className: 'platform-form-section-title', text: config.title || labels[sectionKey] || sectionKey }));
toArray(fields).forEach(function (item) {
children.push(field(item, values || {}));
});
return componentService.element('section', {
className: config.className || 'platform-form-section',
dataset: { formKey: formKey || '', sectionKey: sectionKey || '', moduleKey: form && form.moduleKey || '' }
}, children);
}
function actions(formKey, options) {
var form = formService.get(formKey);
var config = options || {};
if (!form) throw new Error('Form not registered: ' + formKey);
var nodes = toArray(form.actions).map(function (item) {
var record = typeof item === 'string' ? { key: item, label: item } : (item || {});
var key = record.key || record.action;
return componentService.button({
label: record.label || key || '',
icon: record.icon || null,
variant: record.variant || 'primary',
className: record.className || 'btn btn-primary',
action: record.platformAction || (form.moduleKey ? form.moduleKey + '.' + key : key),
dataset: Object.assign({
formKey: formKey,
moduleKey: form.moduleKey || '',
actionKey: key || ''
}, record.dataset || {}),
attrs: record.attrs || {}
});
});
return componentService.element('div', {
className: config.className || 'platform-form-actions',
dataset: { formKey: formKey, moduleKey: form.moduleKey || '' }
}, nodes);
}
function formNode(formKey, values, options) {
var form = formService.get(formKey);
if (!form) throw new Error('Form not registered: ' + formKey);
var config = options || {};
var sections = toArray(config.sections).length ? toArray(config.sections) : formService.sectionOrder();
var children = sections.map(function (sectionKey) {
return section(formKey, sectionKey, values || {}, { title: config.sectionTitle !== false });
});
if (config.actions !== false && toArray(form.actions).length) {
children.push(actions(formKey, config.actionsOptions || {}));
}
return componentService.element('form', {
className: config.className || 'platform-form',
attrs: Object.assign({ novalidate: 'novalidate' }, config.attrs || {}),
dataset: { formKey: form.key, moduleKey: form.moduleKey || '' }
}, children);
}
return {
field: field,
section: section,
actions: actions,
form: formNode,
serialize: formService.serialize
};
}
function createListService() {
var registry = createRegistry('list', 'key');
var defaultPageSize = 20;
function normalizeColumn(column) {
var source = column || {};
return Object.assign({
key: source.key || source.name,
label: source.label || source.key || source.name || '',
sortable: Boolean(source.sortable),
visible: source.visible !== false
}, source);
}
function normalize(record) {
ensureKey(record, 'key');
var columns = toArray(record.columns).map(normalizeColumn).filter(function (column) {
return column.key;
});
return Object.assign({}, record, {
moduleKey: record.moduleKey || record.key,
screenId: record.screenId || record.moduleKey || record.key,
columns: columns,
filters: toArray(record.filters),
search: Object.assign({
enabled: true,
fields: columns.map(function (column) {
return column.key;
})
}, record.search || {}),
pagination: Object.assign({
enabled: true,
pageSize: defaultPageSize,
pageSizeOptions: [10, 20, 50, 100]
}, record.pagination || {}),
rowActions: toArray(record.rowActions),
bulkActions: toArray(record.bulkActions),
states: Object.assign({
loading: 'Dang tai...',
empty: 'Khong co du lieu',
error: 'Khong the tai du lieu'
}, record.states || {})
});
}
function register(record) {
registry.upsert(normalize(record));
return api;
}
function columnsFor(key, options) {
var list = registry.get(key);
if (!list) return [];
var config = options || {};
return toArray(list.columns).filter(function (column) {
return config.includeHidden || column.visible !== false;
});
}
function filtersFor(key) {
var list = registry.get(key);
return list ? toArray(list.filters) : [];
}
function actionsFor(key, scope) {
var list = registry.get(key);
if (!list) return [];
return scope === 'bulk' ? toArray(list.bulkActions) : toArray(list.rowActions);
}
function paginationFor(key) {
var list = registry.get(key);
return list ? Object.assign({}, list.pagination) : null;
}
function queryDefaults(key) {
var pagination = paginationFor(key) || { pageSize: defaultPageSize };
var filters = {};
filtersFor(key).forEach(function (filter) {
var filterKey = filter && (filter.key || filter.name);
if (!filterKey) return;
filters[filterKey] = filter.defaultValue !== undefined ? filter.defaultValue : '';
});
return {
page: 1,
pageSize: pagination.pageSize || defaultPageSize,
search: '',
sort: null,
filters: filters
};
}
var api = {
register: register,
upsert: register,
get: registry.get,
list: registry.list,
columnsFor: columnsFor,
filtersFor: filtersFor,
actionsFor: actionsFor,
paginationFor: paginationFor,
queryDefaults: queryDefaults
};
return api;
}
function createListViewService(componentService, listService) {
function toolbar(listKey, query, options) {
var list = listService.get(listKey);
var config = options || {};
var current = query || listService.queryDefaults(listKey);
var children = [];
if (!list) throw new Error('List not registered: ' + listKey);
if (list.search && list.search.enabled !== false) {
children.push(componentService.searchBox({
name: 'search',
value: current.search || '',
placeholder: list.search.placeholder || config.searchPlaceholder || 'Tim kiem',
dataset: { listKey: listKey }
}));
}
if (list.filters && list.filters.length) {
children.push(componentService.filterBar({
search: false,
filters: list.filters.map(function (filter) {
var key = filter.key || filter.name;
var value = current.filters && current.filters[key] !== undefined ? current.filters[key] : filter.defaultValue;
return Object.assign({}, filter, { value: value });
}),
dataset: { listKey: listKey }
}));
}
return componentService.element('div', {
className: config.className || 'platform-list-toolbar',
dataset: { listKey: listKey, moduleKey: list.moduleKey || '' }
}, children);
}
function table(listKey, rows, options) {
var config = options || {};
var list = listService.get(listKey);
if (!list) throw new Error('List not registered: ' + listKey);
return componentService.table({
columns: listService.columnsFor(listKey, config),
rows: rows || [],
rowKey: config.rowKey || list.rowKey || 'id',
emptyText: list.states && list.states.empty || 'Khong co du lieu',
className: config.className || 'table platform-table',
attrs: { 'data-list-key': listKey }
});
}
function pagination(listKey, meta, options) {
var list = listService.get(listKey);
var config = options || {};
var defaults = listService.queryDefaults(listKey);
var page = meta && meta.page || defaults.page;
var pageSize = meta && meta.pageSize || defaults.pageSize;
var total = meta && meta.total || 0;
if (!list || !list.pagination || list.pagination.enabled === false) {
return componentService.element('div', { className: 'platform-pagination disabled', dataset: { listKey: listKey } });
}
return componentService.pagination({
page: page,
pageSize: pageSize,
total: total,
action: config.action || (list.moduleKey + '.page')
});
}
function actions(listKey, scope, context, options) {
var list = listService.get(listKey);
var config = options || {};
var actionScope = scope || 'row';
var current = context || {};
if (!list) throw new Error('List not registered: ' + listKey);
var nodes = listService.actionsFor(listKey, actionScope).map(function (item) {
var record = typeof item === 'string' ? { key: item, label: item } : (item || {});
var key = record.key || record.action;
var row = current.row || {};
var dataset = Object.assign({
listKey: listKey,
moduleKey: list.moduleKey || '',
actionKey: key || '',
actionScope: actionScope
}, record.dataset || {});
if (row && row.id !== undefined) dataset.rowId = row.id;
return componentService.button({
label: record.label || key || '',
icon: record.icon || null,
variant: record.variant || (actionScope === 'bulk' ? 'light' : 'secondary'),
className: record.className || (actionScope === 'bulk' ? 'btn btn-light' : 'btn btn-sm btn-light'),
action: record.platformAction || (list.moduleKey ? list.moduleKey + '.' + key : key),
dataset: dataset,
attrs: record.attrs || {}
});
});
return componentService.element('div', {
className: config.className || ('platform-list-actions platform-list-' + actionScope + '-actions'),
dataset: { listKey: listKey, moduleKey: list.moduleKey || '', actionScope: actionScope }
}, nodes);
}
function listNode(listKey, rows, state, options) {
var list = listService.get(listKey);
var config = options || {};
if (!list) throw new Error('List not registered: ' + listKey);
var current = state || {};
return componentService.element('section', {
className: config.className || 'platform-list',
dataset: { listKey: listKey, moduleKey: list.moduleKey || '', screenId: list.screenId || '' }
}, [
toolbar(listKey, current.query || {}, config.toolbar || {}),
table(listKey, rows || current.rows || [], config.table || {}),
pagination(listKey, current.meta || {}, config.pagination || {})
]);
}
return {
toolbar: toolbar,
table: table,
pagination: pagination,
actions: actions,
list: listNode
};
}
function createCrudService(routeRegistry, listRegistry, formRegistry, permissionService) {
var registry = createRegistry('crud', 'moduleKey');
var defaultOperations = {
list: true,
detail: true,
create: true,
edit: true,
delete: true,
import: false,
export: false,
log: false
};
var permissionActions = {
list: ACTION.VIEW,
detail: ACTION.VIEW,
create: ACTION.CREATE,
edit: ACTION.EDIT,
delete: ACTION.DELETE,
import: ACTION.IMPORT,
export: ACTION.EXPORT,
log: ACTION.VIEW
};
function normalize(record) {
ensureKey(record, 'moduleKey');
var operations = Object.assign({}, defaultOperations, record.operations || {});
return Object.assign({}, record, {
screenId: record.screenId || record.moduleKey,
listKey: record.listKey || (record.moduleKey + 'List'),
formKey: record.formKey || (record.moduleKey + 'Form'),
detailFormKey: record.detailFormKey || record.formKey || (record.moduleKey + 'Form'),
operations: operations,
rowActions: toArray(record.rowActions),
bulkActions: toArray(record.bulkActions)
});
}
function register(record) {
registry.upsert(normalize(record));
return api;
}
function getConfig(moduleKey) {
return registry.get(moduleKey) || normalize({ moduleKey: moduleKey });
}
function routeFor(moduleKey, operation) {
return routeRegistry.list().find(function (route) {
return route.moduleKey === moduleKey && route.action === operation;
}) || null;
}
function can(moduleKey, operation, context) {
var config = getConfig(moduleKey);
if (!config.operations[operation]) return false;
return permissionService.can(moduleKey, permissionActions[operation] || ACTION.VIEW, context || {});
}
function actionKey(moduleKey, operation) {
return moduleKey + '.' + operation;
}
function operationFor(moduleKey, operation, context) {
var config = getConfig(moduleKey);
var formOperation = operation === 'create' || operation === 'edit' ? operation : 'detail';
var formKey = formOperation === 'detail' ? config.detailFormKey : config.formKey;
return {
moduleKey: moduleKey,
operation: operation,
enabled: Boolean(config.operations[operation]),
allowed: can(moduleKey, operation, context),
route: routeFor(moduleKey, operation),
list: operation === 'list' ? listRegistry.get(config.listKey) : null,
form: formRegistry.get(formKey),
actionKey: actionKey(moduleKey, operation),
permissionAction: permissionActions[operation] || ACTION.VIEW
};
}
function workflowFor(moduleKey, context) {
var config = getConfig(moduleKey);
var workflow = {};
Object.keys(defaultOperations).forEach(function (operation) {
workflow[operation] = operationFor(moduleKey, operation, context);
});
workflow.config = clone(config);
return workflow;
}
function enabledOperations(moduleKey) {
var config = getConfig(moduleKey);
return Object.keys(defaultOperations).filter(function (operation) {
return Boolean(config.operations[operation]);
});
}
var api = {
register: register,
upsert: register,
get: registry.get,
list: registry.list,
can: can,
operationFor: operationFor,
workflowFor: workflowFor,
enabledOperations: enabledOperations
};
return api;
}
function createCrudViewService(componentService, crudService, listViewService, formViewService, modalService) {
function operation(moduleKey, action, context) {
var result = crudService.operationFor(moduleKey, action || 'list', context || {});
if (!result.enabled) throw new Error('CRUD operation disabled: ' + moduleKey + '.' + action);
return result;
}
function header(moduleKey, action, options) {
var config = options || {};
return componentService.element('header', {
className: config.className || 'platform-crud-header',
dataset: { moduleKey: moduleKey, operation: action || 'list' }
}, [
componentService.element('h2', { className: 'platform-crud-title', text: config.title || moduleKey }),
config.subtitle ? componentService.element('p', { className: 'platform-crud-subtitle', text: config.subtitle }) : null
]);
}
function list(moduleKey, rows, state, options) {
var op = operation(moduleKey, 'list', options && options.permissionContext || {});
if (!op.list) throw new Error('List not registered for module: ' + moduleKey);
return componentService.element('section', {
className: options && options.className || 'platform-crud-list-view',
dataset: { moduleKey: moduleKey, operation: 'list', allowed: String(op.allowed) }
}, [
header(moduleKey, 'list', options && options.header || {}),
listViewService.list(op.list.key, rows || [], state || {}, options && options.list || {})
]);
}
function form(moduleKey, action, values, options) {
var op = operation(moduleKey, action || 'detail', options && options.permissionContext || {});
if (!op.form) throw new Error('Form not registered for module: ' + moduleKey);
return componentService.element('section', {
className: options && options.className || 'platform-crud-form-view',
dataset: { moduleKey: moduleKey, operation: action || 'detail', allowed: String(op.allowed) }
}, [
header(moduleKey, action || 'detail', options && options.header || {}),
formViewService.form(op.form.key, values || {}, options && options.form || {})
]);
}
function detail(moduleKey, values, options) {
return form(moduleKey, 'detail', values || {}, options || {});
}
function modal(moduleKey, action, values, options) {
var op = operation(moduleKey, action || 'detail', options && options.permissionContext || {});
if (!op.form) throw new Error('Form not registered for module: ' + moduleKey);
return modalService.renderStandard(Object.assign({
formKey: op.form.key,
title: options && options.title || moduleKey,
subtitle: options && options.subtitle || ''
}, options && options.modal || {}), values || {});
}
function workflow(moduleKey, context) {
return crudService.workflowFor(moduleKey, context || {});
}
return {
operation: operation,
header: header,
list: list,
detail: detail,
form: form,
modal: modal,
workflow: workflow
};
}
function createLayoutService() {
var modes = createRegistry('layoutMode', 'key');
var regions = createRegistry('layoutRegion', 'key');
var breakpoints = {
mobileMax: 820,
tabletMax: 1024
};
function normalizeMode(record) {
ensureKey(record, 'key');
return Object.assign({
regions: [],
navigation: 'sidebar',
modal: 'dialog'
}, record);
}
function registerMode(record) {
modes.upsert(normalizeMode(record));
return api;
}
function registerRegion(record) {
regions.upsert(Object.assign({
required: true,
shared: true
}, record));
return api;
}
function modeFor(width) {
var viewport = Number(width || 0);
if (viewport && viewport <= breakpoints.mobileMax) return modes.get('mobile');
if (viewport && viewport <= breakpoints.tabletMax) return modes.get('tablet');
return modes.get('desktop');
}
function setBreakpoints(values) {
breakpoints = Object.assign({}, breakpoints, values || {});
return api;
}
function regionsFor(modeKey) {
var mode = modes.get(modeKey);
if (!mode) return [];
return toArray(mode.regions).map(function (key) {
return regions.get(key);
}).filter(Boolean);
}
function summary(width) {
var mode = modeFor(width);
return mode ? {
mode: mode.key,
navigation: mode.navigation,
modal: mode.modal,
regions: regionsFor(mode.key).map(function (region) {
return region.key;
})
} : null;
}
var api = {
registerMode: registerMode,
registerRegion: registerRegion,
modes: modes.list,
regions: regions.list,
modeFor: modeFor,
regionsFor: regionsFor,
summary: summary,
setBreakpoints: setBreakpoints,
breakpoints: function () {
return Object.assign({}, breakpoints);
}
};
registerRegion({ key: 'sidebar', selector: '.app-sidebar', role: 'navigation' });
registerRegion({ key: 'content', selector: '.app-content', role: 'main' });
registerRegion({ key: 'bottomNavigation', selector: '.mobile-bottom-nav', role: 'navigation' });
registerRegion({ key: 'modal', selector: '.modal', role: 'dialog' });
registerMode({ key: 'desktop', label: 'Desktop', navigation: 'sidebar', modal: 'dialog', regions: ['sidebar', 'content', 'modal'] });
registerMode({ key: 'tablet', label: 'Tablet', navigation: 'collapsedSidebar', modal: 'dialog', regions: ['sidebar', 'content', 'modal'] });
registerMode({ key: 'mobile', label: 'Mobile', navigation: 'bottomNavigation', modal: 'fullscreen', regions: ['content', 'bottomNavigation', 'modal'] });
return api;
}
function createModalLayoutService(layoutService, appStateService) {
function presentation(options) {
var config = options || {};
var layout = config.layout || (config.width ? layoutService.summary(config.width) : null) || (appStateService && appStateService.get().layout) || layoutService.summary(window.innerWidth || 0);
var mode = layout && layout.mode || 'desktop';
var modal = layout && layout.modal || 'dialog';
return {
mode: mode,
modal: modal,
fullscreen: modal === 'fullscreen',
dialog: modal !== 'fullscreen',
className: modal === 'fullscreen' ? 'modal-fullscreen' : 'modal-dialog'
};
}
function apply(element, options) {
if (!element) return null;
var info = presentation(options || {});
var target = element;
if (element.querySelector && !/(^|\s)modal-dialog(\s|$)/.test(element.className || '')) {
target = element.querySelector('.modal-dialog') || element;
}
target.className = (' ' + (target.className || '') + ' ')
.replace(' modal-fullscreen ', ' ')
.replace(' modal-dialog ', ' ')
.replace(/^\s+|\s+$/g, '');
target.className = (target.className ? target.className + ' ' : '') + info.className;
if (target.dataset) target.dataset.modalPresentation = info.modal;
return info;
}
return {
presentation: presentation,
apply: apply
};
}
function createModalService(componentService, formService, modalLayoutService, formViewService) {
var registry = createRegistry('modal', 'key');
var active = null;
var bridgedApps = [];
var standardSectionOrder = ['basic', 'linked', 'extended', 'history', 'attachments'];
function resolveElement(target) {
if (!target) return null;
if (typeof target === 'string') return document && document.querySelector ? document.querySelector(target) : null;
return target;
}
function bootstrapInstance(element, options) {
if (!element || !window.bootstrap || !window.bootstrap.Modal) return null;
if (typeof window.bootstrap.Modal.getOrCreateInstance === 'function') {
return window.bootstrap.Modal.getOrCreateInstance(element, options || {});
}
if (!element.__thon09BootstrapModal) {
element.__thon09BootstrapModal = new window.bootstrap.Modal(element, options || {});
}
return element.__thon09BootstrapModal;
}
function bootstrapModal(key, target, options) {
return {
key: key,
selector: typeof target === 'string' ? target : null,
element: typeof target === 'string' ? null : target,
open: function (payload) {
var element = this.element || resolveElement(this.selector);
var instance = bootstrapInstance(element, options);
if (!instance) return null;
element && element.dispatchEvent && element.dispatchEvent(new CustomEvent('thon09:modal-open', { detail: { key: key, payload: payload || null } }));
instance.show();
return { key: key, payload: payload || null };
},
close: function () {
var element = this.element || resolveElement(this.selector);
var instance = bootstrapInstance(element, options);
if (!instance) return null;
instance.hide();
element && element.dispatchEvent && element.dispatchEvent(new CustomEvent('thon09:modal-close', { detail: { key: key } }));
return { key: key };
}
};
}
function legacyModal(key, instance) {
return {
key: key,
instance: instance,
open: function (payload) {
if (instance && typeof instance.show === 'function') {
instance.show();
}
return { key: key, payload: payload || null };
},
close: function () {
if (instance && typeof instance.hide === 'function') {
instance.hide();
}
return { key: key };
}
};
}
function standardSchema(config) {
var options = config || {};
var form = options.formKey && formService ? formService.get(options.formKey) : null;
var title = options.title || (form && form.title) || options.key || '';
var sections = Object.assign({
basic: form && form.sections && form.sections.basic || [],
linked: form && form.sections && form.sections.linked || [],
extended: form && form.sections && form.sections.extended || [],
history: [],
attachments: form && form.sections && form.sections.attachments || []
}, options.sections || {});
var tabs = toArray(options.tabs).length ? toArray(options.tabs) : standardSectionOrder.map(function (key) {
var labels = {
basic: 'Thong tin co ban',
linked: 'Thong tin lien ket',
extended: 'Thong tin mo rong',
history: 'Lich su',
attachments: 'Tep dinh kem'
};
return { key: key, label: labels[key], fields: sections[key] || [] };
});
return {
key: options.key || (form && form.modalKey) || (form && form.key) || 'platformModal',
title: title,
subtitle: options.subtitle || '',
formKey: options.formKey || (form && form.key) || null,
moduleKey: options.moduleKey || (form && form.moduleKey) || null,
tabs: tabs,
footerActions: toArray(options.footerActions).length ? toArray(options.footerActions) : toArray(form && form.actions),
payload: clone(options.payload || {})
};
}
function fieldNode(field, payload) {
var node = formViewService
? formViewService.field(field, payload || {})
: componentService.element('div', { className: 'platform-form-field' });
node.className = (node.className || 'platform-form-field').replace('platform-form-field', 'platform-modal-field');
return node;
}
function renderStandard(config, payload) {
if (!componentService) throw new Error('Component service is not available');
var schema = standardSchema(Object.assign({}, config || {}, { payload: payload || config && config.payload || {} }));
var bodyTabs = schema.tabs.map(function (tab) {
return Object.assign({}, tab, {
content: toArray(tab.content).length ? tab.content : toArray(tab.fields).map(function (field) {
return fieldNode(field, schema.payload);
})
});
});
var header = componentService.element('header', { className: 'platform-modal-header' }, [
componentService.element('h2', { className: 'platform-modal-title', text: schema.title }),
schema.subtitle ? componentService.element('p', { className: 'platform-modal-subtitle', text: schema.subtitle }) : null
]);
var body = componentService.tabs({
className: 'platform-modal-tabs',
activeKey: bodyTabs[0] && bodyTabs[0].key,
tabs: bodyTabs
});
var footer = componentService.element('footer', { className: 'platform-modal-footer' }, schema.footerActions.map(function (action) {
return componentService.button({
label: action.label || action.key || action,
variant: action.variant || 'primary',
action: action.action || action.key || action,
dataset: { modalKey: schema.key }
});
}));
var node = componentService.element('section', {
className: 'platform-modal',
attrs: { role: 'dialog', 'aria-modal': 'true' },
dataset: { modalKey: schema.key, moduleKey: schema.moduleKey || '' }
}, [header, body, footer]);
if (modalLayoutService) modalLayoutService.apply(node);
return node;
}
function registerBootstrapAll(root) {
var host = root || document;
if (!host || !host.querySelectorAll) return [];
return Array.prototype.slice.call(host.querySelectorAll('.modal[id]')).map(function (element) {
return registry.upsert(bootstrapModal(element.id, element, {}));
});
}
function attachApp(app) {
if (!app || bridgedApps.indexOf(app) !== -1) return app;
bridgedApps.push(app);
if (!app.modals || typeof app.modals !== 'object') app.modals = {};
registerBootstrapAll(document);
var source = app.modals;
if (typeof Proxy !== 'function') {
Object.keys(source).forEach(function (key) {
registry.upsert(legacyModal(key, source[key]));
});
return app;
}
var target = {};
Object.keys(source).forEach(function (key) {
target[key] = source[key];
registry.upsert(legacyModal(key, source[key]));
});
app.modals = new Proxy(target, {
get: function (store, key) {
return store[key];
},
set: function (store, key, value) {
store[key] = value;
if (typeof key === 'string') {
registry.upsert(legacyModal(key, value));
}
return true;
},
deleteProperty: function (store, key) {
delete store[key];
return true;
},
ownKeys: function (store) {
return Reflect.ownKeys(store);
},
getOwnPropertyDescriptor: function (store, key) {
return Object.getOwnPropertyDescriptor(store, key) || {
configurable: true,
enumerable: true,
writable: true,
value: store[key]
};
}
});
return app;
}
return {
register: registry.register,
upsert: registry.upsert,
get: registry.get,
list: registry.list,
registerBootstrap: function (key, target, options) {
return registry.upsert(bootstrapModal(key, target, options || {}));
},
registerBootstrapAll: registerBootstrapAll,
attachApp: attachApp,
schema: standardSchema,
renderStandard: renderStandard,
open: function (key, payload) {
var modal = registry.get(key);
if (!modal) throw new Error('Modal not registered: ' + key);
active = { key: key, payload: payload || null };
if (typeof modal.open === 'function') return modal.open(payload || null);
return active;
},
close: function (key) {
var target = key || (active && active.key);
if (!target) return null;
var modal = registry.get(target);
active = null;
if (modal && typeof modal.close === 'function') return modal.close();
return null;
},
active: function () {
return active ? clone(active) : null;
}
};
}
function createConfirmDialogService(componentService) {
var active = null;
function normalize(options) {
if (typeof options === 'string') return { message: options };
return options || {};
}
function fallback(config) {
if (window && typeof window.confirm === 'function') {
return Promise.resolve(window.confirm(config.message || config.title || 'Xác nhận thao tác?'));
}
return Promise.resolve(Boolean(config.defaultValue));
}
function ask(options) {
var config = normalize(options);
if (!componentService || !document || !document.body || typeof document.body.appendChild !== 'function') {
return fallback(config);
}
if (active && typeof active.close === 'function') active.close(false);
return new Promise(function (resolve) {
var settled = false;
var tone = config.tone || 'primary';
var title = config.title || 'Xác nhận thao tác';
var message = config.message || '';
var confirmLabel = config.confirmLabel || 'Xác nhận';
var cancelLabel = config.cancelLabel || 'Hủy';
function settle(value) {
if (settled) return;
settled = true;
if (overlay && overlay.parentNode) overlay.parentNode.removeChild(overlay);
document.removeEventListener('keydown', onKeydown);
active = null;
resolve(Boolean(value));
}
function onKeydown(event) {
if (event.key === 'Escape') settle(false);
}
var cancelButton = componentService.button({
label: cancelLabel,
variant: 'light',
className: 'btn btn-light',
on: { click: function () { settle(false); } }
});
var confirmButton = componentService.button({
label: confirmLabel,
variant: tone === 'danger' ? 'danger' : 'primary',
className: 'btn btn-' + (tone === 'danger' ? 'danger' : 'primary'),
on: { click: function () { settle(true); } }
});
var overlay = componentService.element('div', {
className: 'platform-confirm-backdrop',
attrs: { role: 'presentation' },
on: { click: function (event) { if (event.target === overlay) settle(false); } }
}, [
componentService.element('section', {
className: 'platform-confirm-dialog',
attrs: { role: 'dialog', 'aria-modal': 'true', 'aria-labelledby': 'platformConfirmTitle' }
}, [
componentService.element('header', { className: 'platform-confirm-header' }, [
componentService.element('h2', { attrs: { id: 'platformConfirmTitle' }, text: title })
]),
componentService.element('div', { className: 'platform-confirm-body' }, [
componentService.element('p', { text: message })
]),
componentService.element('footer', { className: 'platform-confirm-footer' }, [cancelButton, confirmButton])
])
]);
active = { close: settle };
document.body.appendChild(overlay);
document.addEventListener('keydown', onKeydown);
if (typeof confirmButton.focus === 'function') confirmButton.focus();
});
}
return {
ask: ask,
confirm: ask,
active: function () {
return !!active;
},
close: function (value) {
if (active && typeof active.close === 'function') active.close(value);
}
};
}
function createNavigationExecutorService() {
var last = null;
function controller() {
return window.Thon09NavigationController && typeof window.Thon09NavigationController.navigate === 'function'
? window.Thon09NavigationController
: null;
}
function mirrorApp(state) {
if (!window.App) return false;
window.App.route = state.route;
window.App.moduleKey = state.moduleKey;
window.App.screen = state.screenId;
window.App.action = state.action;
window.App.params = clone(state.params || {});
return true;
}
function execute(state, options) {
var current = Object.assign({}, state || {}, { options: options || {} });
var screen = current.screenId || current.moduleKey;
var targetController = controller();
var controllerResult = null;
if (targetController) {
controllerResult = targetController.navigate(screen, options || {});
}
last = {
screen: screen,
route: current.route,
moduleKey: current.moduleKey,
action: current.action,
controllerAvailable: Boolean(targetController),
controllerResult: controllerResult || null,
appMirrored: mirrorApp(current)
};
return clone(current);
}
return {
execute: execute,
mirrorApp: mirrorApp,
controller: controller,
inspect: function () {
return last ? clone(last) : {
screen: null,
controllerAvailable: Boolean(controller()),
appMirrored: Boolean(window.App)
};
}
};
}
function createNavigationTransitionService() {
var records = [];
var sequence = 0;
var maxRecords = 50;
function summary(state) {
return state ? {
route: state.route || null,
moduleKey: state.moduleKey || null,
screenId: state.screenId || state.moduleKey || null,
action: state.action || null
} : null;
}
function safeClone(value) {
try {
return clone(value);
} catch (error) {
return null;
}
}
function executorSummary(executor) {
return executor ? {
screen: executor.screen || null,
controllerAvailable: Boolean(executor.controllerAvailable),
appMirrored: Boolean(executor.appMirrored)
} : null;
}
function record(state, options, executor) {
var config = options || {};
var transition = Object.assign(summary(state), {
id: ++sequence,
at: Date.now(),
source: config.source || 'navigation',
params: safeClone(state && state.params || {}),
previous: records.length ? summary(records[records.length - 1]) : null,
intent: config.intent ? safeClone(config.intent) : null,
executor: executorSummary(executor)
});
records.push(transition);
if (records.length > maxRecords) records.shift();
return clone(transition);
}
return {
record: record,
current: function () {
return records.length ? clone(records[records.length - 1]) : null;
},
history: function (limit) {
var count = limit || records.length;
return clone(records.slice(Math.max(0, records.length - count)));
},
clear: function () {
records = [];
return true;
},
count: function () {
return records.length;
}
};
}
function createNavigationService(routerService, navigationExecutorService, navigationTransitionService) {
var current = null;
function resolve(target, options) {
return routerService.resolve(target, options || {});
}
function activate(state, options) {
current = navigationExecutorService.execute(state || routerService.current(), options || {});
if (navigationTransitionService) {
current.transition = navigationTransitionService.record(current, options || {}, navigationExecutorService.inspect());
}
return clone(current);
}
function navigate(target, options) {
return activate(routerService.sync(target, options || {}), options || {});
}
function bindHistory(historyService, options) {
if (!historyService || typeof historyService.start !== 'function' || typeof historyService.stop !== 'function') {
throw new Error('RouteHistoryService is required');
}
var config = options || {};
function start() {
return historyService.start(function (state, event) {
activate(state, Object.assign({}, config, {
source: 'popstate',
historyState: event && event.state || null
}));
});
}
return {
start: start,
stop: historyService.stop,
active: historyService.active || function () {
return false;
}
};
}
return {
resolve: resolve,
navigate: navigate,
activate: activate,
bindHistory: bindHistory,
current: function () {
return current ? clone(current) : null;
}
};
}
function createMenuService(menuRegistry, moduleRegistry, routeRegistry) {
function modulesForMenu(menuKey) {
var menu = menuRegistry.get(menuKey);
if (!menu) return [];
return toArray(menu.items).map(function (moduleKey) {
return moduleRegistry.get(moduleKey);
}).filter(Boolean);
}
function menuForModule(moduleKey) {
return menuRegistry.list().find(function (menu) {
return toArray(menu.items).indexOf(moduleKey) !== -1;
}) || null;
}
function routeForModuleAction(moduleKey, action) {
var targetAction = action || 'list';
return routeRegistry.list().find(function (route) {
return route.moduleKey === moduleKey && route.action === targetAction;
}) || null;
}
function resolveMenuItem(moduleKey, action) {
var module = moduleRegistry.get(moduleKey);
if (!module) return null;
var route = routeForModuleAction(moduleKey, action);
var menu = menuForModule(moduleKey);
return {
moduleKey: module.moduleKey,
screenId: module.screenId,
route: route ? route.path : module.path,
action: action || 'list',
menuKey: menu ? menu.key : null,
label: module.label,
icon: module.icon
};
}
return {
modulesForMenu: modulesForMenu,
menuForModule: menuForModule,
routeForModuleAction: routeForModuleAction,
resolveMenuItem: resolveMenuItem
};
}
function createNavigationMappingService(menuRegistry, moduleRegistry, routeRegistry, menuService) {
function issue(code, message, detail) {
return {
code: code,
message: message,
detail: detail || {}
};
}
function routesForModule(moduleKey) {
return routeRegistry.list().filter(function (route) {
return route.moduleKey === moduleKey;
});
}
function auditMenuItem(menu, moduleKey, seen) {
var module = moduleRegistry.get(moduleKey);
var issues = [];
var listRoute = module ? menuService.routeForModuleAction(moduleKey, 'list') : null;
var resolved = module ? menuService.resolveMenuItem(moduleKey, 'list') : null;
var previousMenu = seen[moduleKey];
seen[moduleKey] = menu.key;
if (!module) {
issues.push(issue('menu-module-missing', 'Menu item does not map to a registered module', {
menuKey: menu.key,
moduleKey: moduleKey
}));
}
if (previousMenu) {
issues.push(issue('module-in-multiple-menus', 'Module appears in more than one menu group', {
moduleKey: moduleKey,
menus: [previousMenu, menu.key]
}));
}
if (module && !module.screenId) {
issues.push(issue('module-screen-missing', 'Module does not declare a screenId', {
moduleKey: moduleKey
}));
}
if (module && !module.path) {
issues.push(issue('module-path-missing', 'Module does not declare a path', {
moduleKey: moduleKey
}));
}
if (module && !listRoute) {
issues.push(issue('module-list-route-missing', 'Module does not have a list route', {
moduleKey: moduleKey,
screenId: module.screenId || null
}));
}
if (module && listRoute && listRoute.screenId !== module.screenId) {
issues.push(issue('route-screen-mismatch', 'List route screenId does not match module screenId', {
moduleKey: moduleKey,
moduleScreenId: module.screenId,
routeScreenId: listRoute.screenId,
route: listRoute.path
}));
}
if (module && resolved && resolved.menuKey !== menu.key) {
issues.push(issue('menu-resolution-mismatch', 'Resolved menu does not match owning menu group', {
moduleKey: moduleKey,
expectedMenuKey: menu.key,
resolvedMenuKey: resolved.menuKey
}));
}
return {
menuKey: menu.key,
moduleKey: moduleKey,
exists: Boolean(module),
screenId: module ? module.screenId || null : null,
path: module ? module.path || null : null,
route: listRoute ? listRoute.path : null,
action: listRoute ? listRoute.action : null,
resolved: resolved,
issues: issues
};
}
function audit(options) {
var config = options || {};
var menus = config.menus || menuRegistry.list();
var modules = moduleRegistry.list();
var routes = routeRegistry.list();
var seen = {};
var records = [];
var issues = [];
menus.forEach(function (menu) {
toArray(menu.items).forEach(function (moduleKey) {
var record = auditMenuItem(menu, moduleKey, seen);
records.push(record);
issues = issues.concat(record.issues);
});
});
modules.forEach(function (module) {
if (!seen[module.moduleKey]) {
issues.push(issue('module-menu-missing', 'Module is not assigned to any menu group', {
moduleKey: module.moduleKey,
screenId: module.screenId || null
}));
}
});
routes.forEach(function (route) {
var module = moduleRegistry.get(route.moduleKey);
if (!module) {
issues.push(issue('route-module-missing', 'Route points to an unknown module', {
route: route.path,
moduleKey: route.moduleKey
}));
} else if (route.screenId !== module.screenId) {
issues.push(issue('route-screen-mismatch', 'Route screenId does not match module screenId', {
route: route.path,
moduleKey: route.moduleKey,
moduleScreenId: module.screenId,
routeScreenId: route.screenId
}));
}
if (!route.action) {
issues.push(issue('route-action-missing', 'Route does not declare an action', {
route: route.path,
moduleKey: route.moduleKey
}));
}
});
return {
ok: issues.length === 0,
issues: issues,
menus: menus.map(function (menu) {
return {
key: menu.key,
label: menu.label || '',
itemCount: toArray(menu.items).length
};
}),
records: records,
moduleCount: modules.length,
routeCount: routes.length,
menuItemCount: records.length
};
}
return {
audit: audit,
ok: function (options) {
return audit(options || {}).ok;
},
routesForModule: routesForModule
};
}
function createNavigationDomCoverageService(moduleRegistry, domRootService, navigationMappingService) {
function issue(code, message, detail) {
return {
code: code,
message: message,
detail: detail || {}
};
}
function screenIdFor(node) {
if (!node) return null;
if (node.dataset && node.dataset.screenId) return node.dataset.screenId;
if (node.getAttribute && node.getAttribute('data-screen-id')) return node.getAttribute('data-screen-id');
if (node.id && /-screen$/.test(node.id)) return node.id.replace(/-screen$/, '');
return node.id || null;
}
function targetModules(options) {
var config = options || {};
if (config.moduleKeys) {
return toArray(config.moduleKeys).map(function (moduleKey) {
return moduleRegistry.get(moduleKey);
}).filter(Boolean);
}
return navigationMappingService.audit(config.mappingOptions || {}).records
.filter(function (record) {
return record.exists;
})
.map(function (record) {
return moduleRegistry.get(record.moduleKey);
})
.filter(Boolean);
}
function audit(options) {
var config = options || {};
var screens = domRootService.screens(config);
var screenMap = {};
var issues = [];
var records = [];
screens.forEach(function (node) {
var screenId = screenIdFor(node);
if (!screenId) {
issues.push(issue('screen-id-missing', 'Screen DOM node does not expose a screenId'));
return;
}
if (!screenMap[screenId]) screenMap[screenId] = [];
screenMap[screenId].push(node);
});
Object.keys(screenMap).forEach(function (screenId) {
if (screenMap[screenId].length > 1) {
issues.push(issue('screen-dom-duplicate', 'More than one DOM node maps to the same screenId', {
screenId: screenId,
count: screenMap[screenId].length
}));
}
});
targetModules(config).forEach(function (module) {
var screenId = module.screenId || module.moduleKey;
var count = screenMap[screenId] ? screenMap[screenId].length : 0;
var record = {
moduleKey: module.moduleKey,
screenId: screenId,
present: count > 0,
count: count
};
if (!record.present) {
issues.push(issue('screen-dom-missing', 'Module screenId does not have a matching DOM screen', {
moduleKey: module.moduleKey,
screenId: screenId
}));
}
records.push(record);
});
return {
ok: issues.length === 0,
issues: issues,
records: records,
screenCount: screens.length,
coveredCount: records.filter(function (record) {
return record.present;
}).length,
targetCount: records.length
};
}
return {
audit: audit,
ok: function (options) {
return audit(options || {}).ok;
}
};
}
function createNavigationScopeService(moduleRegistry, menuRegistry) {
var definitions = new Map();
function uniqueKeys(keys) {
var seen = {};
return toArray(keys).map(function (key) {
return String(key || '').trim();
}).filter(function (key) {
if (!key || seen[key]) return false;
seen[key] = true;
return true;
});
}
function issue(code, message, detail) {
return {
code: code,
message: message,
detail: detail || {}
};
}
function register(record) {
ensureKey(record, 'key');
definitions.set(record.key, Object.freeze(Object.assign({}, record, {
moduleKeys: uniqueKeys(record.moduleKeys)
})));
return service;
}
function resolveRecord(scope) {
if (scope === undefined || scope === null || scope === false) return null;
if (Array.isArray(scope)) {
return { key: 'custom', label: 'Custom navigation scope', moduleKeys: scope };
}
if (typeof scope === 'string') {
if (definitions.has(scope)) return definitions.get(scope);
var menu = menuRegistry && menuRegistry.get(scope);
if (menu) {
return { key: 'menu:' + menu.key, label: menu.label, moduleKeys: menu.items || [] };
}
return { key: scope, label: scope, moduleKeys: [], missingScope: true };
}
if (scope && typeof scope === 'object') {
return Object.assign({
key: scope.key || scope.name || 'custom',
label: scope.label || scope.key || scope.name || 'Custom navigation scope'
}, scope);
}
return { key: String(scope), label: String(scope), moduleKeys: [], missingScope: true };
}
function resolve(scope) {
var record = resolveRecord(scope);
if (!record) return null;
var moduleKeys = uniqueKeys(record.moduleKeys);
var issues = [];
var missingModuleKeys = [];
var modules = moduleKeys.map(function (moduleKey) {
var module = moduleRegistry.get(moduleKey);
if (!module) missingModuleKeys.push(moduleKey);
return module;
}).filter(Boolean);
if (record.missingScope) {
issues.push(issue('scope-not-found', 'Navigation rollout scope is not registered', { scope: record.key }));
}
missingModuleKeys.forEach(function (moduleKey) {
issues.push(issue('scope-module-missing', 'Navigation rollout scope references a missing module', {
scope: record.key,
moduleKey: moduleKey
}));
});
return {
key: record.key,
label: record.label || record.key,
moduleKeys: moduleKeys,
modules: modules,
missingModuleKeys: missingModuleKeys,
ok: issues.length === 0,
issues: issues
};
}
var service = {
register: register,
resolve: resolve,
list: function () {
return Array.from(definitions.values()).map(clone);
},
keys: function () {
return Array.from(definitions.keys());
}
};
return service;
}
function createNavigationRouteCoverageService(routeRegistry, moduleRegistry, navigationScopeService) {
var defaultActions = ['list', 'create', 'detail', 'edit'];
function issue(code, message, detail) {
return {
code: code,
message: message,
detail: detail || {}
};
}
function routeActionsFor(moduleKey) {
return routeRegistry.list().filter(function (route) {
return route.moduleKey === moduleKey;
}).map(function (route) {
return route.action || 'list';
});
}
function targetModuleKeys(options) {
var config = options || {};
var scope = navigationScopeService && navigationScopeService.resolve(config.navigationScope || config.rolloutScope || config.scope);
if (scope) {
return {
scope: scope,
moduleKeys: scope.moduleKeys.slice(),
issues: scope.issues.slice()
};
}
return {
scope: null,
moduleKeys: moduleRegistry.list().map(function (module) {
return module.moduleKey;
}),
issues: []
};
}
function audit(options) {
var config = options || {};
var actions = toArray(config.actions).length ? toArray(config.actions) : defaultActions;
var target = targetModuleKeys(config);
var issues = target.issues.slice();
var records = target.moduleKeys.map(function (moduleKey) {
var module = moduleRegistry.get(moduleKey);
var routeActions = routeActionsFor(moduleKey);
var missingActions = actions.filter(function (action) {
return routeActions.indexOf(action) === -1;
});
if (!module) {
issues.push(issue('route-module-missing', 'Route coverage target module is not registered', { moduleKey: moduleKey }));
}
missingActions.forEach(function (action) {
issues.push(issue('route-action-missing', 'Module is missing a required navigation route action', {
moduleKey: moduleKey,
action: action
}));
});
return {
moduleKey: moduleKey,
screenId: module && module.screenId || null,
routeActions: routeActions,
requiredActions: actions.slice(),
missingActions: missingActions
};
});
return {
ok: issues.length === 0,
scope: target.scope,
moduleCount: records.length,
actionCount: actions.length,
coveredCount: records.filter(function (record) {
return record.missingActions.length === 0;
}).length,
requiredActions: actions.slice(),
records: records,
issues: issues
};
}
return {
audit: audit,
ok: function (options) {
return audit(options || {}).ok;
}
};
}
function createModuleMigrationService(moduleRegistry, routeRegistry, moduleLoaderService, crudService, navigationDomCoverageService, navigationScopeService) {
var completions = new Map();
var events = [];
var eventSequence = 0;
var eventLimit = 200;
var stageDefaults = {
navigation: {
routes: ['list'],
dom: true,
loaderConfigured: false,
loaderAvailable: false,
crud: false
},
crud: {
routes: ['list', 'create', 'detail', 'edit'],
dom: true,
loaderConfigured: false,
loaderAvailable: false,
crud: true,
crudOperations: ['list', 'detail', 'create', 'edit']
},
runtime: {
routes: ['list'],
dom: true,
loaderConfigured: false,
loaderAvailable: false,
crud: false
}
};
function issue(code, message, detail) {
return {
code: code,
message: message,
detail: detail || {}
};
}
function requirements(options) {
var config = options || {};
var stage = config.stage || 'navigation';
var defaults = stageDefaults[stage] || stageDefaults.navigation;
return Object.assign({}, defaults, config.require || {}, {
stage: stage,
routes: config.routes || (config.require && config.require.routes) || defaults.routes,
crudOperations: config.crudOperations || (config.require && config.require.crudOperations) || defaults.crudOperations || []
});
}
function routeActions(moduleKey) {
return routeRegistry.list().filter(function (route) {
return route.moduleKey === moduleKey;
}).map(function (route) {
return route.action || 'list';
});
}
function crudReport(moduleKey, requiredOperations) {
var workflow = crudService.workflowFor(moduleKey, {});
var operations = toArray(requiredOperations).map(function (operation) {
var record = workflow[operation] || {};
return {
operation: operation,
enabled: Boolean(record.enabled),
hasRoute: Boolean(record.route),
hasList: operation === 'list' ? Boolean(record.list) : null,
hasForm: operation === 'list' || operation === 'delete' ? null : Boolean(record.form),
permissionAction: record.permissionAction || null
};
});
return {
config: workflow.config,
operations: operations
};
}
function inspectModule(moduleKey, options) {
var config = options || {};
var req = requirements(config);
var module = moduleRegistry.get(moduleKey);
var issues = [];
if (!module) {
return {
ready: false,
moduleKey: moduleKey,
stage: req.stage,
registered: false,
issues: [issue('module-missing', 'Module is not registered', { moduleKey: moduleKey })]
};
}
var actions = routeActions(moduleKey);
var missingRoutes = toArray(req.routes).filter(function (action) {
return actions.indexOf(action) === -1;
});
missingRoutes.forEach(function (action) {
issues.push(issue('route-missing', 'Module is missing a required route action', { moduleKey: moduleKey, action: action }));
});
var loader = moduleLoaderService.inspect(moduleKey, config.loaderOptions || config);
if (req.loaderConfigured && !loader.loaderName) {
issues.push(issue('loader-not-configured', 'Module loaderName is not configured', { moduleKey: moduleKey }));
}
if (req.loaderAvailable && !loader.available) {
issues.push(issue(loader.missingReason || 'loader-unavailable', 'Module loader is not available', {
moduleKey: moduleKey,
loaderName: loader.loaderName
}));
}
var domCoverage = null;
if (req.dom && navigationDomCoverageService && config.document) {
domCoverage = navigationDomCoverageService.audit(Object.assign({}, config, { moduleKeys: [moduleKey] }));
if (!domCoverage.ok) {
domCoverage.issues.forEach(function (domIssue) {
issues.push(issue('dom-' + domIssue.code, domIssue.message, domIssue.detail || {}));
});
}
}
var crud = null;
if (req.crud) {
crud = crudReport(moduleKey, req.crudOperations);
crud.operations.forEach(function (operation) {
if (!operation.enabled) {
issues.push(issue('crud-operation-disabled', 'Required CRUD operation is disabled', {
moduleKey: moduleKey,
operation: operation.operation
}));
}
if (!operation.hasRoute) {
issues.push(issue('crud-route-missing', 'Required CRUD operation route is missing', {
moduleKey: moduleKey,
operation: operation.operation
}));
}
if (operation.hasList === false) {
issues.push(issue('crud-list-missing', 'Required CRUD list metadata is missing', {
moduleKey: moduleKey,
operation: operation.operation
}));
}
if (operation.hasForm === false) {
issues.push(issue('crud-form-missing', 'Required CRUD form metadata is missing', {
moduleKey: moduleKey,
operation: operation.operation
}));
}
});
}
return {
ready: issues.length === 0,
moduleKey: moduleKey,
stage: req.stage,
registered: true,
screenId: module.screenId || null,
path: module.path || null,
routeActions: actions,
requiredRoutes: toArray(req.routes),
missingRoutes: missingRoutes,
loader: loader,
domCoverage: domCoverage,
crud: crud,
issues: issues
};
}
function targetKeys(options) {
var config = options || {};
var scope = navigationScopeService && navigationScopeService.resolve(config.navigationScope || config.rolloutScope || config.scope);
if (scope) return { scope: scope, moduleKeys: scope.moduleKeys.slice(), issues: scope.issues.slice() };
if (config.moduleKeys) return { scope: null, moduleKeys: toArray(config.moduleKeys), issues: [] };
if (config.moduleKey) return { scope: null, moduleKeys: [config.moduleKey], issues: [] };
return {
scope: null,
moduleKeys: moduleRegistry.list().map(function (module) {
return module.moduleKey;
}),
issues: []
};
}
function progressKey(options) {
var config = options || {};
var target = targetKeys(config);
var stage = config.stage || 'navigation';
var scopeKey = target.scope ? target.scope.key : target.moduleKeys.join(',');
return stage + ':' + scopeKey;
}
function storedCompleted(options) {
var values = completions.get(progressKey(options || {})) || [];
return values.slice();
}
function completedModules(options) {
var seen = {};
return storedCompleted(options).concat(toArray(options && options.completedModules)).filter(function (moduleKey) {
if (!moduleKey || seen[moduleKey]) return false;
seen[moduleKey] = true;
return true;
});
}
function recordEvent(type, options, detail) {
var config = options || {};
var target = targetKeys(config);
var event = {
id: ++eventSequence,
type: type,
at: Date.now(),
stage: config.stage || 'navigation',
scope: target.scope ? target.scope.key : null,
progressKey: progressKey(config),
moduleKey: detail && detail.moduleKey || null,
completed: detail && detail.completed !== undefined ? Boolean(detail.completed) : null,
reason: detail && detail.reason || null
};
events.push(event);
if (events.length > eventLimit) events.shift();
return Object.assign({}, event);
}
function timeline(options) {
var config = options || {};
var hasFilter = config.progressKey || config.navigationScope || config.rolloutScope || config.scope || config.moduleKey || config.moduleKeys || config.stage;
var key = config.progressKey || (config.all === true || !hasFilter ? null : progressKey(config));
var records = events.filter(function (event) {
if (key && event.progressKey !== key) return false;
if (config.type && event.type !== config.type) return false;
return true;
});
return {
eventCount: records.length,
events: records.map(function (event) {
return Object.assign({}, event);
})
};
}
function inspect(options) {
var config = options || {};
var target = targetKeys(config);
var records = target.moduleKeys.map(function (moduleKey) {
return inspectModule(moduleKey, config);
});
var issues = target.issues.slice();
records.forEach(function (record) {
record.issues.forEach(function (recordIssue) {
issues.push(recordIssue);
});
});
return {
ready: issues.length === 0,
stage: (options && options.stage) || 'navigation',
scope: target.scope,
moduleCount: records.length,
readyCount: records.filter(function (record) {
return record.ready;
}).length,
blockedCount: records.filter(function (record) {
return !record.ready;
}).length,
records: records,
issues: issues
};
}
function plan(options) {
var config = options || {};
var report = inspect(config);
var completed = {};
completedModules(config).forEach(function (moduleKey) {
completed[moduleKey] = true;
});
var order = toArray(config.order).length ? toArray(config.order) : report.records.map(function (record) {
return record.moduleKey;
});
var orderedRecords = order.map(function (moduleKey) {
return report.records.find(function (record) {
return record.moduleKey === moduleKey;
});
}).filter(Boolean);
report.records.forEach(function (record) {
if (order.indexOf(record.moduleKey) === -1) orderedRecords.push(record);
});
var ready = orderedRecords.filter(function (record) {
return record.ready && !completed[record.moduleKey];
});
var blocked = orderedRecords.filter(function (record) {
return !record.ready && !completed[record.moduleKey];
});
var completedRecords = orderedRecords.filter(function (record) {
return completed[record.moduleKey];
});
var next = ready.length ? ready[0] : null;
return {
ready: report.ready,
stage: report.stage,
scope: report.scope,
moduleCount: orderedRecords.length,
readyCount: ready.length,
blockedCount: blocked.length,
completedCount: completedRecords.length,
nextModuleKey: next ? next.moduleKey : null,
next: next,
readyModules: ready.map(function (record) {
return record.moduleKey;
}),
blockedModules: blocked.map(function (record) {
return record.moduleKey;
}),
completedModules: completedRecords.map(function (record) {
return record.moduleKey;
}),
records: orderedRecords,
issues: report.issues
};
}
function markComplete(moduleKey, options) {
var config = options || {};
var key = progressKey(config);
var current = storedCompleted(config);
if (current.indexOf(moduleKey) === -1) current.push(moduleKey);
completions.set(key, current);
recordEvent('markComplete', config, { moduleKey: moduleKey, completed: true });
return progress(config);
}
function progress(options) {
var config = options || {};
var current = completedModules(config);
return Object.assign({}, plan(Object.assign({}, config, { completedModules: current })), {
progressKey: progressKey(config),
storedCompletedModules: storedCompleted(config)
});
}
function resetProgress(options) {
var config = options || {};
if (config.all === true) completions.clear();
else completions.delete(progressKey(config));
recordEvent('resetProgress', config, { completed: false, reason: config.all === true ? 'all' : 'scope' });
return progress(config);
}
function queue(options) {
var state = progress(options || {});
var modules = state.records.map(function (record) {
var completed = state.completedModules.indexOf(record.moduleKey) !== -1;
var blocked = state.blockedModules.indexOf(record.moduleKey) !== -1;
return {
moduleKey: record.moduleKey,
screenId: record.screenId || null,
path: record.path || null,
status: completed ? 'completed' : blocked ? 'blocked' : 'ready',
isNext: record.moduleKey === state.nextModuleKey,
ready: record.ready,
issueCount: record.issues.length,
issues: record.issues
};
});
var remainingCount = Math.max(0, state.moduleCount - state.completedCount);
return Object.assign({}, state, {
remainingCount: remainingCount,
percentComplete: state.moduleCount ? Math.round((state.completedCount / state.moduleCount) * 100) : 100,
modules: modules,
upcomingModules: modules.filter(function (record) {
return record.status === 'ready';
}).map(function (record) {
return record.moduleKey;
}),
blockedQueue: modules.filter(function (record) {
return record.status === 'blocked';
}),
completedQueue: modules.filter(function (record) {
return record.status === 'completed';
})
});
}
function blockers(options) {
var snapshot = queue(options || {});
var codes = {};
var modules = snapshot.blockedQueue.map(function (record) {
record.issues.forEach(function (item) {
codes[item.code] = (codes[item.code] || 0) + 1;
});
return {
moduleKey: record.moduleKey,
screenId: record.screenId || null,
path: record.path || null,
issueCount: record.issues.length,
codes: record.issues.map(function (item) {
return item.code;
}),
issues: record.issues
};
});
return {
ready: modules.length === 0,
stage: snapshot.stage,
scope: snapshot.scope,
moduleCount: snapshot.moduleCount,
blockedCount: modules.length,
blockerCount: modules.reduce(function (total, record) {
return total + record.issueCount;
}, 0),
modules: modules,
codes: Object.keys(codes).map(function (code) {
return { code: code, count: codes[code] };
}),
issues: snapshot.issues
};
}
function matrix(options) {
var config = options || {};
var scopes = toArray(config.scopes || config.navigationScopes);
if (!scopes.length && (config.navigationScope || config.rolloutScope || config.scope)) {
scopes = [config.navigationScope || config.rolloutScope || config.scope];
}
if (!scopes.length && navigationScopeService && navigationScopeService.keys) {
scopes = navigationScopeService.keys();
}
var stages = toArray(config.stages).length ? toArray(config.stages) : [config.stage || 'navigation'];
var rows = scopes.map(function (scope) {
var cells = stages.map(function (stage) {
var stageConfig = Object.assign({}, config, { navigationScope: scope, stage: stage });
delete stageConfig.scopes;
delete stageConfig.navigationScopes;
delete stageConfig.stages;
var snapshot = queue(stageConfig);
var blocked = blockers(stageConfig);
return {
stage: stage,
progressKey: snapshot.progressKey,
ready: snapshot.ready,
moduleCount: snapshot.moduleCount,
completedCount: snapshot.completedCount,
remainingCount: snapshot.remainingCount,
readyCount: snapshot.readyCount,
blockedCount: snapshot.blockedCount,
percentComplete: snapshot.percentComplete,
nextModuleKey: snapshot.nextModuleKey,
blockerCount: blocked.blockerCount,
issueCount: snapshot.issues.length
};
});
return {
scope: cells[0] ? cells[0].progressKey.split(':').slice(1).join(':') : String(scope),
scopeRecord: cells[0] ? queue(Object.assign({}, config, { navigationScope: scope, stage: stages[0] })).scope : null,
ready: cells.every(function (cell) {
return cell.ready;
}),
stages: cells
};
});
var nextCell = null;
rows.some(function (row) {
return row.stages.some(function (cell) {
if (cell.nextModuleKey) {
nextCell = { scope: row.scope, stage: cell.stage, moduleKey: cell.nextModuleKey };
return true;
}
return false;
});
});
return {
ready: rows.every(function (row) {
return row.ready;
}),
scopeCount: rows.length,
stageCount: stages.length,
stages: stages,
rows: rows,
blockedRows: rows.filter(function (row) {
return !row.ready;
}),
next: nextCell
};
}
function assertMatrix(options) {
var snapshot = matrix(options || {});
if (!snapshot.ready) {
var blocked = [];
snapshot.rows.forEach(function (row) {
row.stages.forEach(function (stage) {
if (!stage.ready) blocked.push(row.scope + ':' + stage.stage);
});
});
throw new Error('Module migration matrix blocked: ' + blocked.join(', '));
}
return snapshot;
}
function report(options) {
var config = options || {};
var stages = toArray(config.stages).length ? toArray(config.stages) : [config.stage || 'navigation'];
var stageReports = stages.map(function (stage) {
var stageConfig = Object.assign({}, config, { stage: stage });
delete stageConfig.stages;
var state = progress(stageConfig);
return {
stage: stage,
progressKey: state.progressKey,
ready: state.ready,
moduleCount: state.moduleCount,
readyCount: state.readyCount,
blockedCount: state.blockedCount,
completedCount: state.completedCount,
nextModuleKey: state.nextModuleKey,
readyModules: state.readyModules,
blockedModules: state.blockedModules,
completedModules: state.completedModules,
storedCompletedModules: state.storedCompletedModules,
issueCount: state.issues.length,
issues: state.issues
};
});
var nextStage = stageReports.find(function (record) {
return record.nextModuleKey;
}) || null;
var blockedStages = stageReports.filter(function (record) {
return !record.ready;
});
var issues = [];
stageReports.forEach(function (record) {
record.issues.forEach(function (item) {
issues.push(item);
});
});
return {
ready: blockedStages.length === 0,
scope: targetKeys(config).scope,
stageCount: stageReports.length,
stages: stageReports,
nextStage: nextStage ? nextStage.stage : null,
nextModuleKey: nextStage ? nextStage.nextModuleKey : null,
blockedStages: blockedStages.map(function (record) {
return record.stage;
}),
issueCount: issues.length,
issues: issues
};
}
function assertReport(options) {
var snapshot = report(options || {});
if (!snapshot.ready) {
throw new Error('Module migration report blocked: ' + snapshot.blockedStages.join(', '));
}
return snapshot;
}
function reports(options) {
var config = options || {};
var scopes = toArray(config.scopes || config.navigationScopes);
if (!scopes.length && (config.navigationScope || config.rolloutScope || config.scope)) {
scopes = [config.navigationScope || config.rolloutScope || config.scope];
}
if (!scopes.length && navigationScopeService && navigationScopeService.keys) {
scopes = navigationScopeService.keys();
}
return scopes.map(function (scope) {
return report(Object.assign({}, config, { navigationScope: scope }));
});
}
function assertReports(options) {
var snapshots = reports(options || {});
var blocked = [];
snapshots.forEach(function (snapshot) {
if (!snapshot.ready) {
var scopeKey = snapshot.scope && snapshot.scope.key || 'unscoped';
snapshot.blockedStages.forEach(function (stage) {
blocked.push(scopeKey + ':' + stage);
});
}
});
if (blocked.length) throw new Error('Module migration reports blocked: ' + blocked.join(', '));
return snapshots;
}
function handoffChecklist(record, req) {
if (!record) return [];
var checks = [];
function add(key, label, ready, detail) {
checks.push({
key: key,
label: label,
ready: Boolean(ready),
detail: detail || {}
});
}
add('registered', 'Module is registered', record.registered, { moduleKey: record.moduleKey });
add('routes', 'Required routes are registered', record.missingRoutes.length === 0, {
requiredRoutes: record.requiredRoutes,
routeActions: record.routeActions,
missingRoutes: record.missingRoutes
});
if (req.dom) {
add('dom', 'Screen DOM is covered', Boolean(record.domCoverage && record.domCoverage.ok), {
screenId: record.screenId,
issueCount: record.domCoverage ? record.domCoverage.issues.length : null
});
}
add('loaderConfigured', 'Module loader is configured', !req.loaderConfigured || Boolean(record.loader && record.loader.loaderName), {
loaderName: record.loader && record.loader.loaderName || null,
required: Boolean(req.loaderConfigured)
});
add('loaderAvailable', 'Module loader is available', !req.loaderAvailable || Boolean(record.loader && record.loader.available), {
loaderName: record.loader && record.loader.loaderName || null,
available: Boolean(record.loader && record.loader.available),
required: Boolean(req.loaderAvailable)
});
if (req.crud && record.crud) {
add('crud', 'Required CRUD operations are ready', record.crud.operations.every(function (operation) {
return operation.enabled && operation.hasRoute && operation.hasList !== false && operation.hasForm !== false;
}), {
operations: record.crud.operations
});
}
return checks;
}
function handoff(options) {
var config = options || {};
var migrationPlan = plan(config);
var moduleKey = config.moduleKey || migrationPlan.nextModuleKey;
if (!moduleKey) {
return {
ready: false,
canMigrate: false,
moduleKey: null,
stage: migrationPlan.stage,
scope: migrationPlan.scope,
nextModuleKey: null,
isNext: false,
checklist: [],
issues: [issue('handoff-module-missing', 'No module is available for migration handoff')]
};
}
var req = requirements(config);
var record = inspectModule(moduleKey, config);
var checklist = handoffChecklist(record, req);
return {
ready: record.ready,
canMigrate: record.ready && checklist.every(function (item) {
return item.ready;
}),
moduleKey: moduleKey,
stage: record.stage,
scope: migrationPlan.scope,
nextModuleKey: migrationPlan.nextModuleKey,
isNext: moduleKey === migrationPlan.nextModuleKey,
screenId: record.screenId || null,
path: record.path || null,
loaderName: record.loader && record.loader.loaderName || null,
checklist: checklist,
record: record,
issueCount: record.issues.length,
issues: record.issues
};
}
function gate(options) {
var config = options || {};
var snapshot = queue(config);
var blocked = blockers(config);
var packet = snapshot.nextModuleKey ? handoff(config) : null;
var canAdvance = Boolean(packet && packet.canMigrate);
var reason = null;
if (!snapshot.nextModuleKey && blocked.blockedCount > 0) reason = 'blocked';
else if (!snapshot.nextModuleKey) reason = 'complete';
else if (!canAdvance) reason = 'handoff-blocked';
return {
canAdvance: canAdvance,
reason: reason,
stage: snapshot.stage,
scope: snapshot.scope,
progressKey: snapshot.progressKey,
nextModuleKey: snapshot.nextModuleKey,
completedCount: snapshot.completedCount,
remainingCount: snapshot.remainingCount,
blockedCount: blocked.blockedCount,
blockerCount: blocked.blockerCount,
queue: snapshot,
blockers: blocked,
handoff: packet
};
}
function current(options) {
var config = options || {};
var gateResult = gate(config);
var history = timeline(config);
var modules = gateResult.queue.modules || [];
var moduleIndex = modules.findIndex(function (record) {
return record.moduleKey === gateResult.nextModuleKey;
});
return {
canAdvance: gateResult.canAdvance,
reason: gateResult.reason,
stage: gateResult.stage,
scope: gateResult.scope,
progressKey: gateResult.progressKey,
moduleKey: gateResult.nextModuleKey,
nextModuleKey: gateResult.nextModuleKey,
queuePosition: moduleIndex === -1 ? null : moduleIndex + 1,
completedCount: gateResult.completedCount,
remainingCount: gateResult.remainingCount,
percentComplete: gateResult.queue.percentComplete,
blockedCount: gateResult.blockedCount,
blockerCount: gateResult.blockerCount,
gate: gateResult,
handoff: gateResult.handoff,
blockers: gateResult.blockers,
queue: gateResult.queue,
timeline: {
eventCount: history.eventCount,
latest: history.events.slice(-5)
}
};
}
function checkpoint(options) {
var config = options || {};
var snapshot = current(config);
var summary = report(config);
var history = timeline(config);
var nextAction = 'complete';
if (snapshot.canAdvance) nextAction = 'advance';
else if (snapshot.reason === 'blocked' || snapshot.reason === 'handoff-blocked') nextAction = 'resolve-blockers';
return {
generatedAt: Date.now(),
nextAction: nextAction,
stage: snapshot.stage,
scope: snapshot.scope,
progressKey: snapshot.progressKey,
moduleKey: snapshot.moduleKey,
canAdvance: snapshot.canAdvance,
reason: snapshot.reason,
current: snapshot,
report: summary,
blockers: snapshot.blockers,
queue: snapshot.queue,
handoff: snapshot.handoff,
timeline: history
};
}
function status(options) {
var config = options || {};
var stages = toArray(config.stages).length ? toArray(config.stages) : [config.stage || 'navigation'];
var scopes = toArray(config.scopes || config.navigationScopes);
var scopeKey = config.navigationScope || config.rolloutScope || config.scope || scopes[0] || null;
var checkpointConfig = Object.assign({}, config, { stage: stages[0] });
delete checkpointConfig.stages;
delete checkpointConfig.scopes;
delete checkpointConfig.navigationScopes;
if (scopeKey) checkpointConfig.navigationScope = scopeKey;
var reportConfig = Object.assign({}, config, { stages: stages });
if (scopeKey) reportConfig.navigationScope = scopeKey;
var matrixConfig = Object.assign({}, config, { stages: stages });
if (scopes.length) matrixConfig.scopes = scopes;
else if (scopeKey) matrixConfig.scopes = [scopeKey];
var point = checkpoint(checkpointConfig);
var summary = report(reportConfig);
var grid = matrix(matrixConfig);
var history = timeline(checkpointConfig);
var ready = summary.ready && grid.ready && (point.canAdvance || point.nextAction === 'complete');
return {
generatedAt: Date.now(),
ready: ready,
blocked: !ready,
nextAction: point.nextAction,
stage: point.stage,
stages: stages,
scope: point.scope,
progressKey: point.progressKey,
moduleKey: point.moduleKey,
canAdvance: point.canAdvance,
reason: point.reason,
issueCount: summary.issueCount,
blockedRows: grid.blockedRows,
checkpoint: point,
report: summary,
matrix: grid,
timeline: history
};
}
function assertStatus(options) {
var snapshot = status(options || {});
if (!snapshot.ready) {
var blocked = [];
snapshot.matrix.rows.forEach(function (row) {
row.stages.forEach(function (stage) {
if (!stage.ready) blocked.push(row.scope + ':' + stage.stage);
});
});
throw new Error('Module migration status blocked: ' + (blocked.length ? blocked.join(', ') : (snapshot.reason || 'unknown')));
}
return snapshot;
}
function assertCheckpoint(options) {
var snapshot = checkpoint(options || {});
if (!snapshot.canAdvance) {
var blockerCodes = snapshot.blockers.codes.map(function (item) {
return item.code;
}).join(', ');
throw new Error('Module migration checkpoint blocked: ' + (snapshot.reason || 'unknown') + (blockerCodes ? ' (' + blockerCodes + ')' : ''));
}
return snapshot;
}
function assertGate(options) {
var result = gate(options || {});
if (!result.canAdvance) {
var blockerCodes = result.blockers.codes.map(function (item) {
return item.code;
}).join(', ');
throw new Error('Module migration gate blocked: ' + (result.reason || 'unknown') + (blockerCodes ? ' (' + blockerCodes + ')' : ''));
}
return result;
}
function completeHandoff(moduleKey, options) {
var config = options || {};
var targetModuleKey = moduleKey || config.moduleKey || null;
var packet = handoff(Object.assign({}, config, targetModuleKey ? { moduleKey: targetModuleKey } : {}));
if (!packet.canMigrate) {
recordEvent('completeHandoff', config, {
moduleKey: packet.moduleKey,
completed: false,
reason: 'handoff-blocked'
});
return {
completed: false,
reason: 'handoff-blocked',
moduleKey: packet.moduleKey,
handoff: packet,
progress: progress(config)
};
}
if (!packet.isNext && config.allowOutOfOrder !== true) {
recordEvent('completeHandoff', config, {
moduleKey: packet.moduleKey,
completed: false,
reason: 'out-of-order'
});
return {
completed: false,
reason: 'out-of-order',
moduleKey: packet.moduleKey,
handoff: packet,
progress: progress(config)
};
}
var nextProgress = markComplete(packet.moduleKey, config);
recordEvent('completeHandoff', config, {
moduleKey: packet.moduleKey,
completed: true
});
return {
completed: true,
reason: null,
moduleKey: packet.moduleKey,
handoff: packet,
progress: nextProgress
};
}
function advance(options) {
return completeHandoff(null, options || {});
}
function assertReady(options) {
var report = inspect(options || {});
if (!report.ready) {
throw new Error('Module migration blocked: ' + report.issues.map(function (item) {
return item.code;
}).join(', '));
}
return report;
}
return {
inspectModule: inspectModule,
inspect: inspect,
plan: plan,
progress: progress,
markComplete: markComplete,
resetProgress: resetProgress,
queue: queue,
blockers: blockers,
matrix: matrix,
assertMatrix: assertMatrix,
report: report,
assertReport: assertReport,
reports: reports,
assertReports: assertReports,
timeline: timeline,
handoff: handoff,
gate: gate,
current: current,
next: current,
checkpoint: checkpoint,
status: status,
assertStatus: assertStatus,
assertCheckpoint: assertCheckpoint,
assertGate: assertGate,
assertCanAdvance: assertGate,
completeHandoff: completeHandoff,
advance: advance,
ready: function (options) {
return inspect(options || {}).ready;
},
assertReady: assertReady
};
}
function createNavigationIntentService(menuService, routerService) {
function datasetValue(node, key) {
if (!node) return null;
if (node.dataset && node.dataset[key] !== undefined) return node.dataset[key];
if (!node.getAttribute) return null;
return node.getAttribute('data-' + key.replace(/[A-Z]/g, function (letter) {
return '-' + letter.toLowerCase();
}));
}
function hrefFor(node) {
if (!node || !node.getAttribute) return null;
var href = node.getAttribute('href');
if (!href || href === '#' || /^javascript:/i.test(href)) return null;
return href;
}
function fromMenu(moduleKey, action, options) {
var config = options || {};
var item = menuService.resolveMenuItem(moduleKey, action || config.action || 'list');
if (!item) return null;
return Object.assign({}, item, {
route: config.route || item.route,
source: config.source || 'menu'
});
}
function fromRoute(route, options) {
if (!route) return null;
var config = options || {};
return Object.assign({}, routerService.resolve(route, config), {
source: config.source || 'route'
});
}
function fromElement(node, options) {
if (!node) return null;
var config = options || {};
var moduleKey = datasetValue(node, 'module') || datasetValue(node, 'moduleKey') || datasetValue(node, 'screen') || datasetValue(node, 'mobileScreen');
var action = datasetValue(node, 'action') || config.action || 'list';
var route = datasetValue(node, 'route') || hrefFor(node) || config.route || null;
if (moduleKey) return fromMenu(moduleKey, action, { route: route, source: config.source || 'element' });
if (route) return fromRoute(route, { source: config.source || 'element' });
return null;
}
function closestElement(node) {
var currentNode = node || null;
while (currentNode) {
if (fromElement(currentNode)) return currentNode;
currentNode = currentNode.parentElement || currentNode.parentNode || null;
}
return null;
}
function fromEvent(event, options) {
var node = closestElement(event && event.target);
return node ? fromElement(node, Object.assign({}, options || {}, { source: 'event' })) : null;
}
return {
fromMenu: fromMenu,
fromRoute: fromRoute,
fromElement: fromElement,
fromEvent: fromEvent,
closestElement: closestElement
};
}
function createNavigationDelegationService(navigationIntentService, navigationService) {
var bindings = [];
function handleClick(event, options) {
var config = options || {};
if (event && event.defaultPrevented && !config.allowDefaultPrevented) return null;
var intent = navigationIntentService.fromEvent(event, config);
if (!intent) return null;
if (event && typeof event.preventDefault === 'function') event.preventDefault();
if (config.stopPropagation && event && typeof event.stopPropagation === 'function') event.stopPropagation();
return navigationService.navigate(intent, Object.assign({}, config, {
source: intent.source,
intent: intent
}));
}
function bind(root, options) {
if (!root || typeof root.addEventListener !== 'function') {
throw new Error('Navigation delegation root is required');
}
var config = options || {};
var handler = function (event) {
return handleClick(event, config);
};
root.addEventListener('click', handler);
var binding = { root: root, handler: handler };
bindings.push(binding);
return function unbind() {
var index = bindings.indexOf(binding);
if (index === -1) return false;
if (typeof root.removeEventListener === 'function') root.removeEventListener('click', handler);
bindings.splice(index, 1);
return true;
};
}
return {
handleClick: handleClick,
bind: bind,
bindingCount: function () {
return bindings.length;
}
};
}
function createBreadcrumbService(routeRegistry, moduleRegistry, menuService) {
var labels = {
list: 'Danh sach',
detail: 'Chi tiet',
create: 'Them moi',
edit: 'Chinh sua',
delete: 'Xoa',
import: 'Import',
export: 'Export',
log: 'Nhat ky'
};
function home() {
return { label: 'Dashboard', route: '/dashboard', moduleKey: 'dashboard', screenId: 'dashboard' };
}
function item(label, route, extra) {
return Object.assign({ label: label || '', route: route || null }, extra || {});
}
function fromModuleAction(moduleKey, action, params) {
var module = moduleRegistry.get(moduleKey);
if (!module) return [home()];
var menu = menuService.menuForModule(moduleKey);
var route = menuService.routeForModuleAction(moduleKey, action || 'list');
var crumbs = [home()];
if (menu && menu.key !== 'dashboard') crumbs.push(item(menu.label, null, { menuKey: menu.key }));
crumbs.push(item(module.label, module.path || (route && route.path), { moduleKey: module.moduleKey, screenId: module.screenId }));
if (action && action !== 'list') {
crumbs.push(item(labels[action] || action, route ? route.path : null, { action: action, params: params || {} }));
}
return crumbs;
}
function fromRoute(path) {
var matched = routeRegistry.match(path || '/dashboard');
if (!matched) return [home()];
return fromModuleAction(matched.moduleKey, matched.action || 'list', matched.params || {});
}
function current(options) {
var config = options || {};
if (config.route) return fromRoute(config.route);
if (config.moduleKey) return fromModuleAction(config.moduleKey, config.action || 'list', config.params || {});
return [home()];
}
function render(root, options) {
var target = root || (document && document.querySelector && document.querySelector('[data-platform-breadcrumb]'));
if (!target) return false;
target.textContent = '';
current(options).forEach(function (crumb, index, crumbs) {
var node = document.createElement(index < crumbs.length - 1 && crumb.route ? 'a' : 'span');
node.textContent = crumb.label;
node.className = index === crumbs.length - 1 ? 'breadcrumb-item active' : 'breadcrumb-item';
if (node.tagName === 'A') node.setAttribute('href', crumb.route);
if (crumb.moduleKey) node.dataset.moduleKey = crumb.moduleKey;
if (crumb.action) node.dataset.action = crumb.action;
target.appendChild(node);
});
target.dataset.platformBreadcrumb = 'true';
return true;
}
return {
labels: function () {
return Object.assign({}, labels);
},
fromRoute: fromRoute,
fromModuleAction: fromModuleAction,
current: current,
render: render
};
}
function createAppStateService(routeRegistry, moduleRegistry, layoutService, breadcrumbService) {
var subscribers = [];
var current = {
route: '/dashboard',
moduleKey: 'dashboard',
screenId: 'dashboard',
action: 'list',
params: {},
layout: layoutService.summary(window.innerWidth || 0),
breadcrumbs: breadcrumbService.current({ route: '/dashboard' }),
updatedAt: Date.now()
};
function emit() {
var snapshot = clone(current);
if (document && document.dispatchEvent) {
document.dispatchEvent(new CustomEvent('thon09:app-state-change', { detail: snapshot }));
}
subscribers.slice().forEach(function (subscriber) {
if (!subscriber.moduleKey || subscriber.moduleKey === snapshot.moduleKey) {
subscriber.callback(clone(snapshot));
}
});
return snapshot;
}
function subscribe(callback, options) {
if (typeof callback !== 'function') {
throw new Error('AppState subscriber callback is required');
}
var config = options || {};
var subscriber = {
callback: callback,
moduleKey: config.moduleKey || null
};
subscribers.push(subscriber);
return function unsubscribe() {
var index = subscribers.indexOf(subscriber);
if (index !== -1) {
subscribers.splice(index, 1);
return true;
}
return false;
};
}
function normalize(update) {
var input = update || {};
var matched = input.route ? routeRegistry.match(input.route) : null;
var moduleKey = input.moduleKey || (matched && matched.moduleKey) || current.moduleKey;
var module = moduleRegistry.get(moduleKey);
var action = input.action || (matched && matched.action) || current.action || 'list';
var params = Object.assign({}, matched && matched.params || {}, input.params || {});
var route = input.route || (matched && matched.path) || (module && module.path) || current.route;
return {
route: route,
moduleKey: moduleKey,
screenId: input.screenId || (module && module.screenId) || moduleKey,
action: action,
params: params,
layout: input.layout || layoutService.summary(input.width || window.innerWidth || 0),
breadcrumbs: input.breadcrumbs || breadcrumbService.current({ route: route, moduleKey: moduleKey, action: action, params: params }),
updatedAt: Date.now()
};
}
function set(update) {
current = normalize(update);
return emit();
}
function patch(update) {
var input = Object.assign({}, current, update || {});
if (!update || !Object.prototype.hasOwnProperty.call(update, 'layout')) delete input.layout;
if (!update || !Object.prototype.hasOwnProperty.call(update, 'breadcrumbs')) delete input.breadcrumbs;
current = Object.assign({}, current, normalize(input));
return emit();
}
function get() {
return clone(current);
}
function reset() {
current = normalize({ route: '/dashboard', moduleKey: 'dashboard', screenId: 'dashboard', action: 'list', params: {} });
return emit();
}
return {
get: get,
set: set,
patch: patch,
reset: reset,
subscribe: subscribe,
onChange: subscribe,
subscriberCount: function () {
return subscribers.length;
}
};
}
function createRouterService(routeRegistry, moduleRegistry, appStateService) {
function normalizeTarget(target) {
if (target && typeof target === 'object') {
return target.route || target.path || target.moduleKey || target.screenId || '/dashboard';
}
return String(target || '/dashboard');
}
function cleanPath(target) {
var value = normalizeTarget(target);
if (/^[A-Za-z][A-Za-z0-9+.-]*:\/\//.test(value)) {
var anchor = document.createElement('a');
anchor.href = value;
value = anchor.pathname || '/';
}
return value.split('#')[0].split('?')[0] || '/';
}
function interpolate(path, params) {
var values = params || {};
return String(path || '/').replace(/:([A-Za-z0-9_]+)/g, function (_, key) {
return encodeURIComponent(values[key] === undefined || values[key] === null ? '' : values[key]);
});
}
function routeFor(moduleKey, action) {
var targetAction = action || 'list';
return routeRegistry.list().find(function (route) {
return route.moduleKey === moduleKey && route.action === targetAction;
}) || null;
}
function pathFor(moduleKey, action, params) {
var module = moduleRegistry.get(moduleKey);
var route = routeFor(moduleKey, action || 'list');
if (route) return interpolate(route.path, params || {});
return module ? module.path || '/' + module.moduleKey : '/' + moduleKey;
}
function resolve(target, options) {
var config = Object.assign({}, target && typeof target === 'object' ? target : {}, options || {});
var rawTarget = normalizeTarget(target);
var path = config.route || config.path || cleanPath(rawTarget);
var module = moduleRegistry.get(rawTarget);
if (module && !config.route && !config.path) path = module.path || '/' + module.moduleKey;
if (config.moduleKey && !config.route && !config.path) path = pathFor(config.moduleKey, config.action || 'list', config.params || {});
var matched = routeRegistry.match(path);
var moduleKey = config.moduleKey || (matched && matched.moduleKey) || (module && module.moduleKey) || rawTarget;
var moduleRecord = moduleRegistry.get(moduleKey);
var action = config.action || (matched && matched.action) || 'list';
var params = Object.assign({}, matched && matched.params || {}, config.params || {});
var route = config.route || config.path || (matched ? interpolate(matched.path, params) : path);
return {
route: route,
routePattern: matched ? matched.path : null,
moduleKey: moduleKey,
screenId: config.screenId || (moduleRecord && moduleRecord.screenId) || moduleKey,
action: action,
params: params
};
}
function sync(target, options) {
var resolved = resolve(target, options);
var config = target && typeof target === 'object' ? target : {};
return appStateService.set(Object.assign({}, config, resolved, options || {}));
}
function current() {
return appStateService.get();
}
return {
routeFor: routeFor,
pathFor: pathFor,
resolve: resolve,
sync: sync,
current: current
};
}
function createRouteHistoryService(routerService) {
var popStateHandler = null;
function canWriteHistory(method) {
return window.history && typeof window.history[method] === 'function';
}
function currentPath() {
if (window.location) {
return (window.location.pathname || '/') + (window.location.search || '') + (window.location.hash || '');
}
return routerService.current().route || '/dashboard';
}
function write(method, target, options) {
var resolved = routerService.resolve(target, options || {});
var state = routerService.sync(resolved, Object.assign({}, options || {}, { historyMethod: method }));
if (canWriteHistory(method)) {
window.history[method]({
route: state.route,
moduleKey: state.moduleKey,
screenId: state.screenId,
action: state.action,
params: state.params
}, '', state.route);
}
return state;
}
function start(callback) {
if (!window.addEventListener || popStateHandler) return false;
popStateHandler = function (event) {
var state = routerService.sync(currentPath(), { historyState: event && event.state || null });
if (typeof callback === 'function') callback(state, event || null);
};
window.addEventListener('popstate', popStateHandler);
return true;
}
function stop() {
if (!popStateHandler || !window.removeEventListener) return false;
window.removeEventListener('popstate', popStateHandler);
popStateHandler = null;
return true;
}
return {
currentPath: currentPath,
push: function (target, options) {
return write('pushState', target, options || {});
},
replace: function (target, options) {
return write('replaceState', target, options || {});
},
start: start,
stop: stop,
active: function () {
return Boolean(popStateHandler);
}
};
}
function createDomRootService(defaultSelectors) {
var selectors = Object.assign({
sidebar: '.gov-nav',
bottomNavigation: '.mobile-bottom-nav',
breadcrumb: '[data-platform-breadcrumb]',
screenRoot: '[data-platform-screen-root]',
screens: '[data-screen-id], .screen'
}, defaultSelectors || {});
function rootDocument(options) {
return options && options.document || document;
}
function selectorFor(key) {
return selectors[key] || null;
}
function query(selector, options) {
var host = rootDocument(options || {});
if (!selector || !host || !host.querySelector) return null;
return host.querySelector(selector);
}
function queryAll(selector, options) {
var host = rootDocument(options || {});
if (!selector || !host || !host.querySelectorAll) return [];
return Array.prototype.slice.call(host.querySelectorAll(selector));
}
function resolve(key, options) {
var config = options || {};
if (config[key]) return config[key];
return query(config.selector || selectorFor(key), config);
}
function screens(options) {
var config = options || {};
if (config.screens) return Array.prototype.slice.call(config.screens);
var root = config.screenRoot || config.root || resolve('screenRoot', config) || rootDocument(config);
if (!root || !root.querySelectorAll) return [];
return Array.prototype.slice.call(root.querySelectorAll(config.selector || selectorFor('screens')));
}
function navigationRoots(options) {
var config = options || {};
var roots = [];
function add(root) {
if (root && roots.indexOf(root) === -1) roots.push(root);
}
toArray(config.navigationRoots).forEach(add);
add(config.navigationRoot);
add(config.sidebarRoot || resolve('sidebar', config));
add(config.bottomRoot || config.mobileRoot || resolve('bottomNavigation', config));
return roots;
}
function shellOptions(options) {
var config = options || {};
return Object.assign({}, config, {
sidebarRoot: config.sidebarRoot || resolve('sidebar', config),
bottomRoot: config.bottomRoot || config.mobileRoot || resolve('bottomNavigation', config),
breadcrumbRoot: config.breadcrumbRoot || resolve('breadcrumb', config),
screenRoot: config.screenRoot || resolve('screenRoot', config),
screens: config.screens || screens(config)
});
}
return {
selectorFor: selectorFor,
selectors: function () {
return clone(selectors);
},
configure: function (nextSelectors) {
selectors = Object.assign({}, selectors, nextSelectors || {});
return clone(selectors);
},
query: query,
queryAll: queryAll,
resolve: resolve,
screens: screens,
navigationRoots: navigationRoots,
shellOptions: shellOptions
};
}
function createNavigationViewService(appStateService, breadcrumbService, domRootService) {
function hasClass(node, className) {
return (' ' + (node.className || '') + ' ').indexOf(' ' + className + ' ') !== -1;
}
function addClass(node, className) {
if (!hasClass(node, className)) node.className = (node.className ? node.className + ' ' : '') + className;
}
function removeClass(node, className) {
node.className = (' ' + (node.className || '') + ' ').replace(' ' + className + ' ', ' ').replace(/^\s+|\s+$/g, '');
}
function setAriaCurrent(node, active) {
if (!node) return;
if (active && node.setAttribute) node.setAttribute('aria-current', 'page');
if (!active && node.removeAttribute) node.removeAttribute('aria-current');
if (!active && node.attributes) delete node.attributes['aria-current'];
}
function updateGroup(root, attribute, screenId) {
var target = root || (domRootService && domRootService.resolve(attribute === 'data-mobile-screen' ? 'bottomNavigation' : 'sidebar'));
if (!target || !target.querySelectorAll) return { total: 0, active: 0 };
var nodes = Array.prototype.slice.call(target.querySelectorAll('[' + attribute + ']'));
var activeCount = 0;
nodes.forEach(function (node) {
var value = node.dataset ? node.dataset[attribute === 'data-mobile-screen' ? 'mobileScreen' : 'screen'] : node.getAttribute && node.getAttribute(attribute);
var active = value === screenId;
if (active) {
activeCount += 1;
addClass(node, 'active');
} else {
removeClass(node, 'active');
}
setAriaCurrent(node, active);
});
if (attribute === 'data-screen') {
target.querySelectorAll('.nav-section').forEach(function (section) {
var hasActive = Boolean(section.querySelector('.nav-link.active[data-screen]'));
if (hasActive) {
addClass(section, 'is-open');
removeClass(section, 'is-collapsed');
var toggle = section.querySelector(':scope > .nav-section-title, :scope > .sidebar-accordion-toggle');
if (toggle) toggle.setAttribute('aria-expanded', 'true');
}
});
}
return { total: nodes.length, active: activeCount };
}
function sync(options) {
var config = options || {};
var state = config.state || appStateService.get();
var screenId = state.screenId || state.moduleKey;
var result = {
screenId: screenId,
sidebar: updateGroup(config.sidebarRoot, 'data-screen', screenId),
bottomNavigation: updateGroup(config.bottomRoot || config.mobileRoot, 'data-mobile-screen', screenId),
breadcrumb: false
};
if (config.breadcrumbRoot || config.renderBreadcrumb) {
result.breadcrumb = breadcrumbService.render(config.breadcrumbRoot || null, state);
}
return result;
}
return {
sync: sync,
syncSidebar: function (root, state) {
return updateGroup(root, 'data-screen', (state || appStateService.get()).screenId);
},
syncBottomNavigation: function (root, state) {
return updateGroup(root, 'data-mobile-screen', (state || appStateService.get()).screenId);
},
syncBreadcrumb: function (root, state) {
return breadcrumbService.render(root || null, state || appStateService.get());
}
};
}
function createScreenViewService(appStateService, domRootService) {
function hasClass(node, className) {
return (' ' + (node.className || '') + ' ').indexOf(' ' + className + ' ') !== -1;
}
function addClass(node, className) {
if (!hasClass(node, className)) node.className = (node.className ? node.className + ' ' : '') + className;
}
function removeClass(node, className) {
node.className = (' ' + (node.className || '') + ' ').replace(' ' + className + ' ', ' ').replace(/^\s+|\s+$/g, '');
}
function screenIdFor(node) {
if (!node) return null;
if (node.dataset && node.dataset.screenId) return node.dataset.screenId;
if (node.dataset && node.dataset.screen) return node.dataset.screen;
if (node.id) return String(node.id).replace(/-screen$/, '');
return null;
}
function collectScreens(root, screens) {
if (screens) return Array.prototype.slice.call(screens);
if (domRootService) return domRootService.screens({ root: root });
var target = root || document;
if (!target || !target.querySelectorAll) return [];
return Array.prototype.slice.call(target.querySelectorAll('[data-screen-id], .screen'));
}
function hide(node) {
if (!node) return;
if (!node.style) node.style = {};
node.style.display = 'none';
node.style.zIndex = '';
removeClass(node, 'active');
if (node.setAttribute) node.setAttribute('aria-hidden', 'true');
}
function show(node) {
if (!node) return;
if (!node.style) node.style = {};
node.style.display = 'block';
node.style.zIndex = '1';
addClass(node, 'active');
if (node.setAttribute) node.setAttribute('aria-hidden', 'false');
}
function sync(options) {
var config = options || {};
var state = config.state || appStateService.get();
var targetScreen = config.screenId || state.screenId || state.moduleKey;
var nodes = collectScreens(config.root, config.screens);
var shown = null;
nodes.forEach(function (node) {
if (screenIdFor(node) === targetScreen) {
shown = node;
show(node);
} else {
hide(node);
}
});
return {
screenId: targetScreen,
total: nodes.length,
shown: shown ? screenIdFor(shown) : null,
hidden: shown ? nodes.length - 1 : nodes.length
};
}
return {
sync: sync,
hide: hide,
show: show,
screenIdFor: screenIdFor,
collectScreens: collectScreens
};
}
function createNavigationDiagnosticsService(appStateService, navigationTransitionService, navigationExecutorService, domRootService, screenViewService) {
function hasClass(node, className) {
return (' ' + (node && node.className || '') + ' ').indexOf(' ' + className + ' ') !== -1;
}
function ariaHidden(node) {
if (!node) return null;
if (node.attributes && Object.prototype.hasOwnProperty.call(node.attributes, 'aria-hidden')) return String(node.attributes['aria-hidden']);
return node.getAttribute ? node.getAttribute('aria-hidden') : null;
}
function zIndexFor(node) {
var value = node && node.style ? parseInt(node.style.zIndex, 10) : 0;
return isNaN(value) ? 0 : value;
}
function stateSummary(state) {
return state ? {
route: state.route || null,
moduleKey: state.moduleKey || null,
screenId: state.screenId || state.moduleKey || null,
action: state.action || null
} : null;
}
function screenRecord(node) {
var display = node && node.style ? node.style.display || '' : '';
var hidden = ariaHidden(node);
return {
screenId: screenViewService.screenIdFor(node),
display: display,
visible: display !== 'none' && hidden !== 'true',
active: hasClass(node, 'active'),
zIndex: zIndexFor(node),
ariaHidden: hidden
};
}
function navigationRecord(node, attribute) {
var value = node && node.dataset
? node.dataset[attribute === 'data-mobile-screen' ? 'mobileScreen' : 'screen']
: node && node.getAttribute && node.getAttribute(attribute);
return {
screenId: value || null,
active: hasClass(node, 'active'),
ariaCurrent: node && node.attributes ? node.attributes['aria-current'] || null : null
};
}
function inspectScreens(options) {
var config = options || {};
var nodes = config.screens
? Array.prototype.slice.call(config.screens)
: domRootService.screens(config);
if (!nodes.length && screenViewService.collectScreens) {
nodes = screenViewService.collectScreens(config.screenRoot || config.root, config.screens);
}
var records = nodes.map(screenRecord);
var visible = records.filter(function (record) {
return record.visible;
});
var active = records.filter(function (record) {
return record.active;
});
var highest = records.reduce(function (selected, record) {
return !selected || record.zIndex > selected.zIndex ? record : selected;
}, null);
return {
total: records.length,
visibleCount: visible.length,
visibleScreens: visible.map(function (record) {
return record.screenId;
}),
activeScreens: active.map(function (record) {
return record.screenId;
}),
highestZIndexScreen: highest ? highest.screenId : null,
highestZIndex: highest ? highest.zIndex : null,
screens: records
};
}
function inspectNavigation(root, attribute, key, options) {
var target = root || domRootService.resolve(key, options || {});
if (!target || !target.querySelectorAll) return { total: 0, active: 0, activeScreens: [], items: [] };
var records = Array.prototype.slice.call(target.querySelectorAll('[' + attribute + ']')).map(function (node) {
return navigationRecord(node, attribute);
});
var active = records.filter(function (record) {
return record.active;
});
return {
total: records.length,
active: active.length,
activeScreens: active.map(function (record) {
return record.screenId;
}),
items: records
};
}
function inspect(options) {
var config = options || {};
return {
state: stateSummary(config.state || appStateService.get()),
transition: navigationTransitionService.current(),
executor: navigationExecutorService.inspect(),
screens: inspectScreens(config),
sidebar: inspectNavigation(config.sidebarRoot, 'data-screen', 'sidebar', config),
bottomNavigation: inspectNavigation(config.bottomRoot || config.mobileRoot, 'data-mobile-screen', 'bottomNavigation', config)
};
}
return {
inspect: inspect,
inspectScreens: inspectScreens,
inspectNavigation: inspectNavigation
};
}
function createNavigationGuardService(navigationDiagnosticsService) {
function issue(code, message, detail) {
return {
code: code,
message: message,
detail: detail || null
};
}
function hasExpected(values, expected) {
return values.indexOf(expected) !== -1;
}
function validate(options) {
var config = options || {};
var report = config.report || navigationDiagnosticsService.inspect(config);
var expectedScreen = config.screenId || report.state && report.state.screenId || null;
var issues = [];
if (!expectedScreen) {
issues.push(issue('missing-expected-screen', 'Expected screen is not available from state or options'));
}
if (report.screens.visibleCount !== 1) {
issues.push(issue('visible-screen-count', 'Exactly one screen must be visible', {
visibleCount: report.screens.visibleCount,
visibleScreens: report.screens.visibleScreens
}));
}
if (expectedScreen && report.screens.visibleCount === 1 && report.screens.visibleScreens[0] !== expectedScreen) {
issues.push(issue('visible-screen-mismatch', 'Visible screen does not match expected screen', {
expected: expectedScreen,
visible: report.screens.visibleScreens[0]
}));
}
if (expectedScreen && !hasExpected(report.screens.activeScreens, expectedScreen)) {
issues.push(issue('active-screen-mismatch', 'Active screen does not match expected screen', {
expected: expectedScreen,
activeScreens: report.screens.activeScreens
}));
}
if (expectedScreen && report.screens.highestZIndexScreen && report.screens.highestZIndexScreen !== expectedScreen) {
issues.push(issue('z-index-screen-mismatch', 'Highest z-index screen does not match expected screen', {
expected: expectedScreen,
highest: report.screens.highestZIndexScreen
}));
}
if (report.sidebar.total > 0) {
if (report.sidebar.active !== 1 || expectedScreen && !hasExpected(report.sidebar.activeScreens, expectedScreen)) {
issues.push(issue('sidebar-active-mismatch', 'Sidebar active item does not match expected screen', {
expected: expectedScreen,
active: report.sidebar.activeScreens
}));
}
}
if (report.bottomNavigation.total > 0) {
if (report.bottomNavigation.active !== 1 || expectedScreen && !hasExpected(report.bottomNavigation.activeScreens, expectedScreen)) {
issues.push(issue('bottom-navigation-active-mismatch', 'Bottom navigation active item does not match expected screen', {
expected: expectedScreen,
active: report.bottomNavigation.activeScreens
}));
}
}
if (expectedScreen && report.executor && report.executor.screen && report.executor.screen !== expectedScreen) {
issues.push(issue('executor-screen-mismatch', 'Navigation executor screen does not match expected screen', {
expected: expectedScreen,
executor: report.executor.screen
}));
}
return {
ok: issues.length === 0,
expectedScreen: expectedScreen,
issues: issues,
report: report
};
}
function assertValid(options) {
var result = validate(options || {});
if (!result.ok) {
throw new Error('Navigation guard failed: ' + result.issues.map(function (item) {
return item.code;
}).join(', '));
}
return result;
}
return {
validate: validate,
assert: assertValid
};
}
function createNavigationReadinessService(domRootService, navigationExecutorService) {
function issue(code, message) {
return { code: code, message: message };
}
function rootStatus(key, options) {
var root = domRootService.resolve(key, options || {});
return {
key: key,
present: Boolean(root),
platformMenu: Boolean(root && root.dataset && root.dataset.platformMenu === 'true')
};
}
function inspect(options) {
var config = options || {};
var sidebar = rootStatus('sidebar', config);
var bottomNavigation = rootStatus('bottomNavigation', config);
var breadcrumb = rootStatus('breadcrumb', config);
var screenRoot = rootStatus('screenRoot', config);
var screenCount = domRootService.screens(config).length;
var executor = navigationExecutorService.inspect();
var issues = [];
if (!executor.controllerAvailable) issues.push(issue('controller-missing', 'Thon09NavigationController is not available'));
if (!window.App) issues.push(issue('app-missing', 'window.App is not available'));
if (!sidebar.present) issues.push(issue('sidebar-root-missing', 'Sidebar navigation root is missing'));
if (!bottomNavigation.present) issues.push(issue('bottom-navigation-root-missing', 'Bottom navigation root is missing'));
if (!screenCount) issues.push(issue('screens-missing', 'No screen nodes were found'));
if (sidebar.present && !sidebar.platformMenu) issues.push(issue('sidebar-menu-not-rendered', 'Sidebar menu was not rendered by platform'));
if (bottomNavigation.present && !bottomNavigation.platformMenu) issues.push(issue('bottom-navigation-menu-not-rendered', 'Bottom navigation was not rendered by platform'));
return {
ready: issues.length === 0,
issues: issues,
controllerAvailable: executor.controllerAvailable,
appAvailable: Boolean(window.App),
roots: {
sidebar: sidebar,
bottomNavigation: bottomNavigation,
breadcrumb: breadcrumb,
screenRoot: screenRoot
},
menuRendered: {
sidebar: sidebar.platformMenu,
bottomNavigation: bottomNavigation.platformMenu
},
screenCount: screenCount
};
}
return {
inspect: inspect,
ready: function (options) {
return inspect(options || {}).ready;
}
};
}
function createNavigationRuntimePlanService(navigationReadinessService, navigationGuardService, domRootService, navigationMappingService, navigationDomCoverageService, navigationScopeService, navigationRouteCoverageService) {
function issue(code, message, detail) {
return {
code: code,
message: message,
detail: detail || {}
};
}
function navigationRootSummary(root, index) {
return {
index: index,
present: Boolean(root),
platformMenu: Boolean(root && root.dataset && root.dataset.platformMenu === 'true'),
itemCount: root && root.querySelectorAll ? root.querySelectorAll('[data-screen], [data-mobile-screen], [data-route], [data-module], [data-module-key]').length : 0
};
}
function plan(options) {
var input = options || {};
var config = domRootService && input.resolveRoots !== false ? domRootService.shellOptions(input) : input;
var scope = navigationScopeService ? navigationScopeService.resolve(input.navigationScope || input.rolloutScope || input.scope) : null;
var domCoverageOptions = Object.assign({}, input.domCoverageOptions || {});
if (scope && scope.moduleKeys.length && !domCoverageOptions.moduleKeys) {
domCoverageOptions.moduleKeys = scope.moduleKeys.slice();
}
var readiness = navigationReadinessService.inspect(config);
var mapping = navigationMappingService && input.mapping !== false ? navigationMappingService.audit(input.mappingOptions || {}) : null;
var domCoverage = navigationDomCoverageService && input.domCoverage !== false
? navigationDomCoverageService.audit(Object.assign({}, config, domCoverageOptions))
: null;
var routeCoverage = navigationRouteCoverageService && input.routeCoverage !== false && scope
? navigationRouteCoverageService.audit(Object.assign({}, input.routeCoverageOptions || {}, { navigationScope: scope }))
: null;
var navigationRoots = domRootService.navigationRoots(config);
var guard = readiness.ready && input.validate !== false ? navigationGuardService.validate(config) : null;
var issues = readiness.issues.slice();
if (scope && !scope.ok) {
scope.issues.forEach(function (scopeIssue) {
issues.push(issue('scope-' + scopeIssue.code, scopeIssue.message, scopeIssue.detail || {}));
});
}
if (mapping && !mapping.ok) {
mapping.issues.forEach(function (mappingIssue) {
issues.push(issue('mapping-' + mappingIssue.code, mappingIssue.message, mappingIssue.detail || {}));
});
}
if (domCoverage && !domCoverage.ok) {
domCoverage.issues.forEach(function (coverageIssue) {
issues.push(issue('dom-coverage-' + coverageIssue.code, coverageIssue.message, coverageIssue.detail || {}));
});
}
if (routeCoverage && !routeCoverage.ok) {
routeCoverage.issues.forEach(function (coverageIssue) {
issues.push(issue('route-coverage-' + coverageIssue.code, coverageIssue.message, coverageIssue.detail || {}));
});
}
if (input.bindNavigation !== false && navigationRoots.length === 0) {
issues.push(issue('navigation-roots-missing', 'No navigation roots are available for runtime binding'));
}
if (guard && !guard.ok) {
guard.issues.forEach(function (guardIssue) {
issues.push(issue('guard-' + guardIssue.code, guardIssue.message, guardIssue.detail || {}));
});
}
return {
canStart: issues.length === 0,
issues: issues,
readiness: readiness,
mapping: mapping,
domCoverage: domCoverage,
routeCoverage: routeCoverage,
scope: scope,
guard: guard,
roots: {
sidebar: readiness.roots.sidebar,
bottomNavigation: readiness.roots.bottomNavigation,
breadcrumb: readiness.roots.breadcrumb,
screenRoot: readiness.roots.screenRoot,
navigation: navigationRoots.map(navigationRootSummary)
},
bindings: {
shell: input.shell !== false,
navigation: input.bindNavigation !== false,
history: input.history === true
},
screenCount: readiness.screenCount
};
}
return {
plan: plan,
canStart: function (options) {
return plan(options || {}).canStart;
}
};
}
function createAppShellViewService(appStateService, screenViewService, navigationViewService) {
function render(options) {
var config = options || {};
var state = config.state || appStateService.get();
var screenResult = screenViewService.sync({
root: config.screenRoot,
screens: config.screens,
state: state,
screenId: config.screenId
});
var navigationResult = navigationViewService.sync({
sidebarRoot: config.sidebarRoot,
bottomRoot: config.bottomRoot,
mobileRoot: config.mobileRoot,
breadcrumbRoot: config.breadcrumbRoot,
renderBreadcrumb: config.renderBreadcrumb,
state: state
});
return {
state: clone(state),
screen: screenResult,
navigation: navigationResult
};
}
function bind(options) {
var config = options || {};
var active = true;
var last = null;
var subscription = config.subscription || config.subscribe || (config.moduleKey ? { moduleKey: config.moduleKey } : {});
function renderState(state) {
if (!active) return last;
last = render(Object.assign({}, config, { state: state || appStateService.get() }));
return last;
}
var unsubscribe = appStateService.subscribe(function (state) {
renderState(state);
}, subscription);
function destroy() {
if (!active) return false;
active = false;
return unsubscribe();
}
renderState(config.state || appStateService.get());
return {
render: renderState,
current: function () {
return last;
},
destroy: destroy,
unsubscribe: destroy
};
}
return {
render: render,
bind: bind
};
}
function createNavigationRuntimeService(navigationDelegationService, navigationService, historyService, shellViewService, domRootService) {
var active = false;
var cleanups = [];
function addCleanup(cleanup) {
if (typeof cleanup === 'function') cleanups.push(cleanup);
}
function navigationRoots(config) {
var roots = [];
function add(root) {
if (root && roots.indexOf(root) === -1) roots.push(root);
}
toArray(config.navigationRoots).forEach(add);
if (config.navigationRoot) add(config.navigationRoot);
if (config.sidebarRoot) add(config.sidebarRoot);
if (config.bottomRoot) add(config.bottomRoot);
if (config.mobileRoot) add(config.mobileRoot);
return roots;
}
function start(options) {
if (active) return false;
var config = options || {};
if (domRootService && config.resolveRoots !== false) {
config = domRootService.shellOptions(config);
}
active = true;
cleanups = [];
if (config.shell !== false) {
var shellBinding = shellViewService.bind(config.shellOptions ? Object.assign({}, config, config.shellOptions) : config);
addCleanup(shellBinding.destroy);
}
if (config.bindNavigation !== false) {
navigationRoots(config).forEach(function (root) {
addCleanup(navigationDelegationService.bind(root, config.delegation || {}));
});
}
if (config.history === true) {
var historyBinding = navigationService.bindHistory(historyService, config.historyOptions || {});
historyBinding.start();
addCleanup(historyBinding.stop);
}
return true;
}
function stop() {
if (!active) return false;
cleanups.slice().reverse().forEach(function (cleanup) {
cleanup();
});
cleanups = [];
active = false;
return true;
}
function inspect() {
return {
active: active,
delegatedBindings: navigationDelegationService.bindingCount(),
historyActive: historyService.active()
};
}
return {
start: start,
stop: stop,
active: function () {
return active;
},
inspect: inspect
};
}
function createNavigationActivationService(navigationRuntimePlanService, navigationRuntimeService) {
var lastPlan = null;
function prepare(options) {
lastPlan = Object.assign({
preparedAt: Date.now()
}, navigationRuntimePlanService.plan(options || {}));
return lastPlan;
}
function activate(options) {
var config = options || {};
var plan = prepare(config);
if (!plan.canStart) {
return {
started: false,
active: navigationRuntimeService.active(),
reason: 'plan-blocked',
plan: plan
};
}
if (navigationRuntimeService.active()) {
return {
started: false,
active: true,
reason: 'already-active',
plan: plan
};
}
return {
started: navigationRuntimeService.start(config),
active: navigationRuntimeService.active(),
reason: null,
plan: plan
};
}
return {
prepare: prepare,
activate: activate,
deactivate: function () {
return navigationRuntimeService.stop();
},
currentPlan: function () {
return lastPlan;
}
};
}
function createNavigationRolloutService(navigationRuntimePlanService, navigationActivationService, navigationRuntimeService) {
function planSummary(plan) {
return plan ? {
canStart: Boolean(plan.canStart),
issueCount: toArray(plan.issues).length,
scope: plan.scope ? {
key: plan.scope.key,
ok: Boolean(plan.scope.ok),
moduleKeys: plan.scope.moduleKeys.slice()
} : null,
preparedAt: plan.preparedAt || null
} : null;
}
function inspect(options) {
var plan = navigationRuntimePlanService.plan(options || {});
var runtime = navigationRuntimeService.inspect();
var lastPlan = navigationActivationService.currentPlan();
return {
ready: Boolean(plan.canStart),
blocked: !plan.canStart,
canActivate: Boolean(plan.canStart && !runtime.active),
active: Boolean(runtime.active),
issues: plan.issues.slice(),
issueCount: plan.issues.length,
plan: plan,
runtime: runtime,
prepared: Boolean(lastPlan),
lastPlan: planSummary(lastPlan)
};
}
function assertReady(options) {
var status = inspect(options || {});
if (!status.ready) {
throw new Error('Navigation rollout blocked: ' + status.issues.map(function (item) {
return item.code;
}).join(', '));
}
return status;
}
return {
inspect: inspect,
ready: function (options) {
return inspect(options || {}).ready;
},
canActivate: function (options) {
return inspect(options || {}).canActivate;
},
assertReady: assertReady
};
}
function createMenuRenderer(menuRegistry, moduleRegistry, menuService) {
var mobileScreens = [
'dashboard',
'households',
'persons',
'publicAssets',
'houses',
'businessHouseholds',
'agriculture',
'livestock',
'contributions',
'gis',
'reports'
];
var sidebarGroupState = new Map();
function sidebarGroupStorageKey(key) {
return 'thon09_sidebar_group_' + key;
}
function readSidebarGroupState(key) {
if (sidebarGroupState.has(key)) return sidebarGroupState.get(key);
var storedOpen = null;
try {
storedOpen = window.localStorage ? window.localStorage.getItem(sidebarGroupStorageKey(key)) : null;
} catch (error) {}
var isOpen = storedOpen === null ? key === 'dashboard' : storedOpen === '1';
sidebarGroupState.set(key, isOpen);
return isOpen;
}
function writeSidebarGroupState(key, isOpen) {
sidebarGroupState.set(key, Boolean(isOpen));
try {
if (window.localStorage) window.localStorage.setItem(sidebarGroupStorageKey(key), isOpen ? '1' : '0');
} catch (error) {}
}
function applySidebarSectionState(section, isOpen) {
if (!section) return false;
section.classList.toggle('is-open', Boolean(isOpen));
section.classList.toggle('is-collapsed', !isOpen);
var toggle = section.querySelector(':scope > .sidebar-accordion-toggle, :scope > .nav-section-title');
if (toggle) toggle.setAttribute('aria-expanded', isOpen ? 'true' : 'false');
return true;
}
function toggleSidebarSection(toggle) {
var section = toggle && toggle.closest ? toggle.closest('.nav-section') : null;
var key = toggle && toggle.dataset ? toggle.dataset.sidebarGroupToggle : '';
if (!section || !key) return false;
var nextOpen = !section.classList.contains('is-open');
writeSidebarGroupState(key, nextOpen);
return applySidebarSectionState(section, nextOpen);
}
function bindSidebarAccordion(root) {
if (!root || root.dataset.sidebarAccordionBound === 'true') return;
root.dataset.sidebarAccordionBound = 'true';
root.addEventListener('click', function (event) {
var toggle = event.target && event.target.closest ? event.target.closest('[data-sidebar-group-toggle]') : null;
if (!toggle || !root.contains(toggle)) return;
event.preventDefault();
event.stopPropagation();
toggleSidebarSection(toggle);
});
root.addEventListener('keydown', function (event) {
if (event.key !== 'Enter' && event.key !== ' ') return;
var toggle = event.target && event.target.closest ? event.target.closest('[data-sidebar-group-toggle]') : null;
if (!toggle || !root.contains(toggle)) return;
event.preventDefault();
event.stopPropagation();
toggleSidebarSection(toggle);
});
}
function icon(className) {
var el = document.createElement('i');
el.className = 'fa-solid ' + className;
el.setAttribute('aria-hidden', 'true');
return el;
}
function buttonFor(module, options) {
var config = options || {};
var button = document.createElement('button');
var label = config.label || module.mobileLabel || module.label || module.moduleKey;
var intent = menuService.resolveMenuItem(module.moduleKey, config.action || 'list') || {};
button.type = 'button';
button.className = config.className || 'nav-link';
if (config.mobile) button.dataset.mobileScreen = module.screenId;
else button.dataset.screen = module.screenId;
button.dataset.module = module.moduleKey;
button.dataset.route = intent.route || module.path || '';
button.dataset.action = intent.action || 'list';
button.appendChild(icon(config.icon || module.icon || 'fa-circle'));
var span = document.createElement('span');
span.textContent = label;
if (config.mobile) span.className = 'mobile-bottom-label';
button.appendChild(span);
return button;
}
function renderSection(menu) {
var section = document.createElement('div');
var isOpen = readSidebarGroupState(menu.key);
section.className = 'nav-section sidebar-accordion-section';
section.dataset.navSection = menu.key;
section.classList.toggle('is-open', isOpen);
section.classList.toggle('is-collapsed', !isOpen);
var title = document.createElement('button');
title.type = 'button';
title.className = 'nav-section-title sidebar-accordion-toggle';
title.dataset.mobileMenuGroupToggle = '';
title.dataset.sidebarGroupToggle = menu.key;
title.setAttribute('aria-expanded', isOpen ? 'true' : 'false');
title.appendChild(icon(menu.icon || 'fa-folder'));
var label = document.createElement('span');
label.textContent = menu.label;
title.appendChild(label);
var caret = document.createElement('b');
caret.className = 'nav-section-caret sidebar-accordion-caret';
caret.setAttribute('aria-hidden', 'true');
caret.textContent = '\u25be';
title.appendChild(caret);
section.appendChild(title);
var list = document.createElement('div');
list.id = 'sidebar-section-' + menu.key;
list.className = 'nav-section-items sidebar-accordion-panel';
title.setAttribute('aria-controls', list.id);
menuService.modulesForMenu(menu.key).forEach(function (module) {
list.appendChild(buttonFor(module));
});
section.appendChild(list);
return section;
}
function renderSidebar(root) {
var nav = root || document.querySelector('.gov-nav');
if (!nav) return false;
bindSidebarAccordion(nav);
nav.textContent = '';
menuRegistry.list().forEach(function (menu) {
nav.appendChild(renderSection(menu));
});
nav.dataset.platformMenu = 'true';
return true;
}
function renderMobile(root) {
var nav = root || document.querySelector('.mobile-bottom-nav');
if (!nav) {
nav = document.createElement('nav');
nav.className = 'mobile-bottom-nav';
nav.setAttribute('aria-label', 'Dieu huong chinh tren di dong');
document.body.appendChild(nav);
}
nav.textContent = '';
mobileScreens.forEach(function (moduleKey) {
var module = moduleRegistry.get(moduleKey);
if (module) nav.appendChild(buttonFor(module, { mobile: true, label: module.mobileLabel || module.label }));
});
nav.dataset.platformMenu = 'true';
return true;
}
function renderAll() {
var rendered = {
sidebar: renderSidebar(),
mobile: renderMobile()
};
if (typeof window.thon09ApplyModuleDisplayOrder === 'function') {
window.thon09ApplyModuleDisplayOrder();
}
return rendered;
}
return {
renderSidebar: renderSidebar,
renderMobile: renderMobile,
renderAll: renderAll,
mobileModules: function () {
return mobileScreens.map(function (moduleKey) {
return moduleRegistry.get(moduleKey);
}).filter(Boolean);
},
mobileScreens: function () {
return mobileScreens.slice();
}
};
}
var routes = createRouteRegistry();
var menus = createRegistry('menu', 'key');
var modules = createRegistry('module', 'moduleKey');
var permissions = createPermissionService();
var state = createStateService();
var moduleLoader = createModuleLoaderService(modules, state, permissions);
var actions = createActionService();
var components = createComponentService(actions, state);
var permissionView = createPermissionViewService(permissions, components);
var forms = createFormService();
var formView = createFormViewService(components, forms);
var lists = createListService();
var listView = createListViewService(components, lists);
var crud = createCrudService(routes, lists, forms, permissions);
var layout = createLayoutService();
var api = createApiClient();
var menu = createMenuService(menus, modules, routes);
var navigationMapping = createNavigationMappingService(menus, modules, routes, menu);
var breadcrumbs = createBreadcrumbService(routes, modules, menu);
var appState = createAppStateService(routes, modules, layout, breadcrumbs);
var modalLayout = createModalLayoutService(layout, appState);
var modals = createModalService(components, forms, modalLayout, formView);
var confirmDialog = createConfirmDialogService(components);
var crudView = createCrudViewService(components, crud, listView, formView, modals);
var router = createRouterService(routes, modules, appState);
var navigationIntent = createNavigationIntentService(menu, router);
var apiResources = createApiResourceService(api, router, crud);
var crudData = createCrudDataService(apiResources, state);
var history = createRouteHistoryService(router);
var navigationExecutor = createNavigationExecutorService();
var navigationTransitions = createNavigationTransitionService();
var navigation = createNavigationService(router, navigationExecutor, navigationTransitions);
var navigationDelegation = createNavigationDelegationService(navigationIntent, navigation);
var domRoots = createDomRootService();
var navigationScopes = createNavigationScopeService(modules, menus);
var navigationRouteCoverage = createNavigationRouteCoverageService(routes, modules, navigationScopes);
var navigationDomCoverage = createNavigationDomCoverageService(modules, domRoots, navigationMapping);
var moduleMigration = createModuleMigrationService(modules, routes, moduleLoader, crud, navigationDomCoverage, navigationScopes);
var navigationView = createNavigationViewService(appState, breadcrumbs, domRoots);
var screens = createScreenViewService(appState, domRoots);
var navigationDiagnostics = createNavigationDiagnosticsService(appState, navigationTransitions, navigationExecutor, domRoots, screens);
var navigationGuard = createNavigationGuardService(navigationDiagnostics);
var navigationReadiness = createNavigationReadinessService(domRoots, navigationExecutor);
var navigationRuntimePlan = createNavigationRuntimePlanService(navigationReadiness, navigationGuard, domRoots, navigationMapping, navigationDomCoverage, navigationScopes, navigationRouteCoverage);
var shellView = createAppShellViewService(appState, screens, navigationView);
var navigationRuntime = createNavigationRuntimeService(navigationDelegation, navigation, history, shellView, domRoots);
var navigationActivation = createNavigationActivationService(navigationRuntimePlan, navigationRuntime);
var navigationRollout = createNavigationRolloutService(navigationRuntimePlan, navigationActivation, navigationRuntime);
var menuRenderer = createMenuRenderer(menus, modules, menu);
var platform = {
version: '2.0.0-phase2',
STATE: STATE,
ACTION: ACTION,
routes: routes,
menus: menus,
modules: modules,
moduleLoader: moduleLoader,
permissions: permissions,
permissionView: permissionView,
state: state,
actions: actions,
components: components,
forms: forms,
formView: formView,
lists: lists,
listView: listView,
crud: crud,
crudView: crudView,
crudData: crudData,
layout: layout,
modalLayout: modalLayout,
modals: modals,
confirmDialog: confirmDialog,
api: api,
apiResources: apiResources,
navigation: navigation,
navigationExecutor: navigationExecutor,
navigationTransitions: navigationTransitions,
navigationDiagnostics: navigationDiagnostics,
navigationGuard: navigationGuard,
navigationReadiness: navigationReadiness,
navigationRuntimePlan: navigationRuntimePlan,
navigationActivation: navigationActivation,
navigationRollout: navigationRollout,
navigationMapping: navigationMapping,
navigationScopes: navigationScopes,
navigationRouteCoverage: navigationRouteCoverage,
navigationDomCoverage: navigationDomCoverage,
moduleMigration: moduleMigration,
navigationIntent: navigationIntent,
navigationDelegation: navigationDelegation,
menu: menu,
breadcrumbs: breadcrumbs,
appState: appState,
router: router,
history: history,
domRoots: domRoots,
navigationView: navigationView,
screens: screens,
shellView: shellView,
navigationRuntime: navigationRuntime,
menuRenderer: menuRenderer,
normalizeApiResponse: normalizeApiResponse,
createRegistry: createRegistry,
createApiClient: createApiClient,
createApiResourceService: createApiResourceService,
createCrudDataService: createCrudDataService,
createModuleLoaderService: createModuleLoaderService,
createPermissionViewService: createPermissionViewService,
createFormViewService: createFormViewService,
createListViewService: createListViewService,
createCrudViewService: createCrudViewService,
createModalService: createModalService,
createConfirmDialogService: createConfirmDialogService
};
function registerDefaults() {
function registerCrudRoutes(moduleKey, path) {
var module = modules.get(moduleKey);
if (!module || !path) return;
[
{ path: path, moduleKey: moduleKey, screenId: module.screenId, action: 'list' },
{ path: path + '/create', moduleKey: moduleKey, screenId: module.screenId, action: 'create' },
{ path: path + '/:id', moduleKey: moduleKey, screenId: module.screenId, action: 'detail' },
{ path: path + '/:id/edit', moduleKey: moduleKey, screenId: module.screenId, action: 'edit' }
].forEach(function (route) {
routes.upsert(route);
});
}
[
{ moduleKey: 'dashboard', screenId: 'dashboard', path: '/dashboard', label: 'Dashboard', icon: 'fa-gauge-high', permissionScope: 'dashboard', loaderName: 'loadDashboard' },
{ moduleKey: 'dashboardHouseholds', screenId: 'dashboardHouseholds', path: '/dashboard/households', label: 'Hộ dân', icon: 'fa-house-chimney', permissionScope: 'dashboard' },
{ moduleKey: 'dashboardPopulation', screenId: 'dashboardPopulation', path: '/dashboard/population', label: 'Nhân khẩu', icon: 'fa-users', permissionScope: 'dashboard' },
{ moduleKey: 'dashboardBusiness', screenId: 'dashboardBusiness', path: '/dashboard/business', label: 'Kinh doanh', icon: 'fa-store', permissionScope: 'dashboard' },
{ moduleKey: 'dashboardVehicles', screenId: 'dashboardVehicles', path: '/dashboard/vehicles', label: 'Xe cộ', icon: 'fa-car', permissionScope: 'dashboard' },
{ moduleKey: 'dashboardLivestock', screenId: 'dashboardLivestock', path: '/dashboard/livestock', label: 'Vật nuôi', icon: 'fa-paw', permissionScope: 'dashboard' },
{ moduleKey: 'dashboardGis', screenId: 'dashboardGis', path: '/dashboard/gis', label: 'GIS', icon: 'fa-map-location-dot', permissionScope: 'dashboard' },
{ moduleKey: 'dashboardReports', screenId: 'dashboardReports', path: '/dashboard/reports', label: 'Báo cáo', icon: 'fa-file-lines', permissionScope: 'dashboard' },
{ moduleKey: 'operationCenter', screenId: 'operationCenter', path: '/operation-center', label: 'Trung tâm điều hành', icon: 'fa-tower-broadcast', permissionScope: 'operationCenter', loaderName: 'loadOperationCenter' },
{ moduleKey: 'households', screenId: 'households', path: '/households', label: 'Hộ gia đình', mobileLabel: 'Hộ', icon: 'fa-house-chimney', permissionScope: 'households', loaderName: 'loadHouseholds' },
{ moduleKey: 'persons', screenId: 'persons', path: '/persons', label: 'Nhân khẩu', mobileLabel: 'Nhân khẩu', icon: 'fa-users', permissionScope: 'persons', loaderName: 'loadPersons' },
{ moduleKey: 'temporaryResidence', screenId: 'temporaryResidence', path: '/temporary-residence', label: 'Tạm trú', mobileLabel: 'Tạm trú', icon: 'fa-location-dot', permissionScope: 'temporaryResidence', loaderName: 'loadTemporaryResidence' },
{ moduleKey: 'temporaryAbsence', screenId: 'temporaryAbsence', path: '/temporary-absence', label: 'Tạm vắng', mobileLabel: 'Tạm vắng', icon: 'fa-person-walking-arrow-right', permissionScope: 'temporaryAbsence', loaderName: 'loadTemporaryAbsence' },
{ moduleKey: 'movements', screenId: 'movements', path: '/movements', label: 'Biến động nhân khẩu', mobileLabel: 'Biến động', icon: 'fa-right-left', permissionScope: 'movements', loaderName: 'loadMovements' },
{ moduleKey: 'publicAssets', screenId: 'publicAssets', path: '/public-assets', label: 'Công trình công cộng', mobileLabel: 'Công trình', icon: 'fa-building-columns', permissionScope: 'publicAssets', loaderName: 'loadPublicAssets' },
{ moduleKey: 'houses', screenId: 'houses', path: '/houses', label: 'Nhà ở và Công trình', mobileLabel: 'Nhà ở', icon: 'fa-building-user', permissionScope: 'houses', loaderName: 'loadHouses' },
{ moduleKey: 'businessHouseholds', screenId: 'businessHouseholds', path: '/household-businesses', label: 'Hộ sản xuất và kinh doanh', mobileLabel: 'Kinh doanh', icon: 'fa-store', permissionScope: 'businessHouseholds', loaderName: 'loadHouseholdBusiness' },
{ moduleKey: 'agriculture', screenId: 'agriculture', path: '/agriculture', label: 'Sản xuất nông nghiệp', mobileLabel: 'Nông nghiệp', icon: 'fa-seedling', permissionScope: 'agriculture', loaderName: 'loadAgriculture' },
{ moduleKey: 'livestock', screenId: 'livestock', path: '/livestock', label: 'Quản lý vật nuôi', mobileLabel: 'Vật nuôi', icon: 'fa-paw', permissionScope: 'livestock', loaderName: 'loadLivestock' },
{ moduleKey: 'vehicles', screenId: 'vehicles', path: '/vehicles', label: 'Quản lý xe cộ', mobileLabel: 'Xe cộ', icon: 'fa-car', permissionScope: 'vehicles' },
{ moduleKey: 'contributions', screenId: 'contributions', path: '/contributions', label: 'Đóng góp hộ', mobileLabel: 'Đóng góp', icon: 'fa-hand-holding-dollar', permissionScope: 'contributions' },
{ moduleKey: 'gis', screenId: 'gis', path: '/gis', label: 'GIS', icon: 'fa-map-location-dot', permissionScope: 'gis', loaderName: 'loadGisMap' },
{ moduleKey: 'reports', screenId: 'reports', path: '/reports', label: 'Báo cáo', icon: 'fa-chart-pie', permissionScope: 'reports', loaderName: 'thon09ViewReport' },
{ moduleKey: 'import', screenId: 'import', path: '/import', label: 'Import dữ liệu', icon: 'fa-file-import', permissionScope: 'import' },
{ moduleKey: 'exportExcel', screenId: 'exportExcel', path: '/export-excel', label: 'Xuất Excel', icon: 'fa-file-export', permissionScope: 'export' },
{ moduleKey: 'printForms', screenId: 'printForms', path: '/print-forms', label: 'In biểu mẫu', icon: 'fa-print', permissionScope: 'reports' },
{ moduleKey: 'systemAdmin', screenId: 'systemAdmin', path: '/system/admin', label: 'Quản trị hệ thống', icon: 'fa-screwdriver-wrench', permissionScope: 'system', loaderName: 'loadSystemAdmin' },
{ moduleKey: 'users', screenId: 'users', path: '/system/users', label: 'Quản lý tài khoản', icon: 'fa-user-shield', permissionScope: 'users', loaderName: 'loadAdminUsers' },
{ moduleKey: 'permissions', screenId: 'permissions', path: '/system/permissions', label: 'Phân quyền', icon: 'fa-key', permissionScope: 'permissions', loaderName: 'loadPermissions' },
{ moduleKey: 'logs', screenId: 'logs', path: '/system/logs', label: 'Nhật ký', icon: 'fa-clock-rotate-left', permissionScope: 'logs', loaderName: 'loadAdminLogs' },
{ moduleKey: 'backups', screenId: 'backups', path: '/system/backups', label: 'Sao lưu', icon: 'fa-database', permissionScope: 'backups', loaderName: 'loadAdminBackups' },
{ moduleKey: 'restore', screenId: 'restore', path: '/system/restore', label: 'Khôi phục', icon: 'fa-rotate-left', permissionScope: 'backups' },
{ moduleKey: 'settings', screenId: 'settings', path: '/system/settings', label: 'Cấu hình hệ thống', icon: 'fa-gear', permissionScope: 'settings', loaderName: 'loadSettings' },
{ moduleKey: 'appearance', screenId: 'appearance', path: '/system/appearance', label: 'Cấu hình giao diện', icon: 'fa-palette', permissionScope: 'settings', loaderName: 'loadAppearanceSettings' }
].forEach(function (module) {
modules.register(module);
});
[
{ path: '/', moduleKey: 'dashboard', screenId: 'dashboard', action: 'list' },
{ path: '/dashboard', moduleKey: 'dashboard', screenId: 'dashboard', action: 'list' },
{ path: '/dashboard/households', moduleKey: 'dashboardHouseholds', screenId: 'dashboardHouseholds', action: 'list' },
{ path: '/dashboard/population', moduleKey: 'dashboardPopulation', screenId: 'dashboardPopulation', action: 'list' },
{ path: '/dashboard/business', moduleKey: 'dashboardBusiness', screenId: 'dashboardBusiness', action: 'list' },
{ path: '/dashboard/vehicles', moduleKey: 'dashboardVehicles', screenId: 'dashboardVehicles', action: 'list' },
{ path: '/dashboard/livestock', moduleKey: 'dashboardLivestock', screenId: 'dashboardLivestock', action: 'list' },
{ path: '/dashboard/gis', moduleKey: 'dashboardGis', screenId: 'dashboardGis', action: 'list' },
{ path: '/dashboard/reports', moduleKey: 'dashboardReports', screenId: 'dashboardReports', action: 'list' },
{ path: '/operation-center', moduleKey: 'operationCenter', screenId: 'operationCenter', action: 'list' },
{ path: '/households', moduleKey: 'households', screenId: 'households', action: 'list' },
{ path: '/households/create', moduleKey: 'households', screenId: 'households', action: 'create' },
{ path: '/households/:id', moduleKey: 'households', screenId: 'households', action: 'detail' },
{ path: '/households/:id/edit', moduleKey: 'households', screenId: 'households', action: 'edit' },
{ path: '/persons', moduleKey: 'persons', screenId: 'persons', action: 'list' },
{ path: '/temporary-residence', moduleKey: 'temporaryResidence', screenId: 'temporaryResidence', action: 'list' },
{ path: '/temporary-absence', moduleKey: 'temporaryAbsence', screenId: 'temporaryAbsence', action: 'list' },
{ path: '/movements', moduleKey: 'movements', screenId: 'movements', action: 'list' },
{ path: '/public-assets', moduleKey: 'publicAssets', screenId: 'publicAssets', action: 'list' },
{ path: '/household-businesses', moduleKey: 'businessHouseholds', screenId: 'businessHouseholds', action: 'list' },
{ path: '/livestock', moduleKey: 'livestock', screenId: 'livestock', action: 'list' },
{ path: '/houses', moduleKey: 'houses', screenId: 'houses', action: 'list' },
{ path: '/vehicles', moduleKey: 'vehicles', screenId: 'vehicles', action: 'list' },
{ path: '/agriculture', moduleKey: 'agriculture', screenId: 'agriculture', action: 'list' },
{ path: '/contributions', moduleKey: 'contributions', screenId: 'contributions', action: 'list' },
{ path: '/gis', moduleKey: 'gis', screenId: 'gis', action: 'list' },
{ path: '/reports', moduleKey: 'reports', screenId: 'reports', action: 'list' },
{ path: '/import', moduleKey: 'import', screenId: 'import', action: 'list' },
{ path: '/export-excel', moduleKey: 'exportExcel', screenId: 'exportExcel', action: 'list' },
{ path: '/print-forms', moduleKey: 'printForms', screenId: 'printForms', action: 'list' },
{ path: '/system/admin', moduleKey: 'systemAdmin', screenId: 'systemAdmin', action: 'list' },
{ path: '/system/users', moduleKey: 'users', screenId: 'users', action: 'list' },
{ path: '/system/permissions', moduleKey: 'permissions', screenId: 'permissions', action: 'list' },
{ path: '/system/logs', moduleKey: 'logs', screenId: 'logs', action: 'list' },
{ path: '/system/backups', moduleKey: 'backups', screenId: 'backups', action: 'list' },
{ path: '/system/restore', moduleKey: 'restore', screenId: 'restore', action: 'list' },
{ path: '/system/settings', moduleKey: 'settings', screenId: 'settings', action: 'list' },
{ path: '/system/appearance', moduleKey: 'appearance', screenId: 'appearance', action: 'list' }
].forEach(function (route) {
routes.register(route);
});
[
'households',
'persons',
'temporaryResidence',
'temporaryAbsence',
'movements',
'publicAssets',
'businessHouseholds',
'livestock',
'houses',
'vehicles',
'agriculture',
'contributions'
].forEach(function (moduleKey) {
var module = modules.get(moduleKey);
if (module) registerCrudRoutes(module.moduleKey, module.path);
});
forms.register({
key: 'householdsForm',
moduleKey: 'households',
modalKey: 'householdModal',
sections: {
basic: [
{ name: 'code', label: 'Mã hộ', required: true },
{ name: 'headName', label: 'Chủ hộ' },
{ name: 'address', label: 'Địa chỉ' }
],
linked: [
{ name: 'areaName', label: 'Địa bàn' }
],
extended: [
{ name: 'memberCount', label: 'Số nhân khẩu', type: 'number', defaultValue: 0 },
{ name: 'status', label: 'Trạng thái' }
]
},
actions: [
{ key: 'save', label: 'Lưu', variant: 'success' },
{ key: 'cancel', label: 'Hủy', variant: 'light' }
]
});
lists.register({
key: 'householdsList',
moduleKey: 'households',
screenId: 'households',
columns: [
{ key: 'code', label: 'Mã hộ', sortable: true },
{ key: 'headName', label: 'Chủ hộ', sortable: true },
{ key: 'address', label: 'Địa chỉ' },
{ key: 'memberCount', label: 'Nhân khẩu', sortable: true },
{ key: 'status', label: 'Trạng thái' }
],
filters: [
{ key: 'areaId', label: 'Địa bàn', type: 'select', defaultValue: '' },
{ key: 'status', label: 'Trạng thái', type: 'select', defaultValue: '' }
],
rowActions: ['detail', 'edit'],
bulkActions: ['export']
});
crud.register({
moduleKey: 'households',
formKey: 'householdsForm',
detailFormKey: 'householdsForm',
listKey: 'householdsList',
rowActions: ['detail', 'edit'],
bulkActions: ['export']
});
forms.register({
key: 'personsForm',
moduleKey: 'persons',
modalKey: 'personModal',
sections: {
basic: [
{ name: 'citizenCode', label: 'Mã nhân khẩu', required: true },
{ name: 'fullName', label: 'Họ tên', required: true },
{ name: 'gender', label: 'Giới tính' },
{ name: 'birthDate', label: 'Ngày sinh', type: 'date' }
],
linked: [
{ name: 'householdCode', label: 'Mã hộ' },
{ name: 'relationship', label: 'Quan hệ với chủ hộ' }
],
extended: [
{ name: 'phone', label: 'Điện thoại' },
{ name: 'status', label: 'Trạng thái' }
]
},
actions: [
{ key: 'save', label: 'Lưu', variant: 'success' },
{ key: 'cancel', label: 'Hủy', variant: 'light' }
]
});
lists.register({
key: 'personsList',
moduleKey: 'persons',
screenId: 'persons',
columns: [
{ key: 'citizenCode', label: 'Mã nhân khẩu', sortable: true },
{ key: 'fullName', label: 'Họ tên', sortable: true },
{ key: 'gender', label: 'Giới tính' },
{ key: 'birthDate', label: 'Ngày sinh', sortable: true },
{ key: 'householdCode', label: 'Mã hộ' },
{ key: 'status', label: 'Trạng thái' }
],
filters: [
{ key: 'gender', label: 'Giới tính', type: 'select', defaultValue: '' },
{ key: 'status', label: 'Trạng thái', type: 'select', defaultValue: '' }
],
rowActions: ['detail', 'edit'],
bulkActions: ['export']
});
crud.register({
moduleKey: 'persons',
formKey: 'personsForm',
detailFormKey: 'personsForm',
listKey: 'personsList',
rowActions: ['detail', 'edit'],
bulkActions: ['export']
});
[
{
moduleKey: 'temporaryResidence',
formKey: 'temporaryResidenceForm',
listKey: 'temporaryResidenceList',
modalKey: 'temporaryResidenceModal',
fields: [
{ name: 'personName', label: 'Nhân khẩu', required: true },
{ name: 'householdCode', label: 'Mã hộ' },
{ name: 'fromDate', label: 'Từ ngày', type: 'date' },
{ name: 'toDate', label: 'Đến ngày', type: 'date' },
{ name: 'temporaryAddress', label: 'Địa chỉ tạm trú' },
{ name: 'status', label: 'Trạng thái' }
]
},
{
moduleKey: 'temporaryAbsence',
formKey: 'temporaryAbsenceForm',
listKey: 'temporaryAbsenceList',
modalKey: 'temporaryAbsenceModal',
fields: [
{ name: 'personName', label: 'Nhân khẩu', required: true },
{ name: 'householdCode', label: 'Mã hộ' },
{ name: 'fromDate', label: 'Từ ngày', type: 'date' },
{ name: 'toDate', label: 'Đến ngày', type: 'date' },
{ name: 'destination', label: 'Nơi đến' },
{ name: 'reason', label: 'Lý do' }
]
},
{
moduleKey: 'movements',
formKey: 'movementsForm',
listKey: 'movementsList',
modalKey: 'movementModal',
fields: [
{ name: 'personName', label: 'Nhân khẩu', required: true },
{ name: 'movementType', label: 'Loại biến động' },
{ name: 'movementDate', label: 'Ngày biến động', type: 'date' },
{ name: 'fromAddress', label: 'Nơi đi' },
{ name: 'toAddress', label: 'Nơi đến' },
{ name: 'status', label: 'Trạng thái' }
]
},
{
moduleKey: 'publicAssets',
formKey: 'publicAssetsForm',
listKey: 'publicAssetsList',
modalKey: 'publicAssetModal',
fields: [
{ name: 'assetCode', label: 'Mã công trình', required: true },
{ name: 'assetName', label: 'Tên công trình', required: true },
{ name: 'category', label: 'Loại công trình' },
{ name: 'location', label: 'Vị trí' },
{ name: 'managedBy', label: 'Đơn vị quản lý' },
{ name: 'status', label: 'Trạng thái' }
]
},
{
moduleKey: 'businessHouseholds',
formKey: 'businessHouseholdsForm',
listKey: 'businessHouseholdsList',
modalKey: 'businessHouseholdModal',
fields: [
{ name: 'householdCode', label: 'Mã hộ', required: true },
{ name: 'ownerName', label: 'Chủ hộ' },
{ name: 'businessName', label: 'Tên cơ sở' },
{ name: 'sector', label: 'Ngành nghề' },
{ name: 'workerCount', label: 'Lao động', type: 'number', defaultValue: 0 },
{ name: 'status', label: 'Trạng thái' }
]
},
{
moduleKey: 'livestock',
formKey: 'livestockForm',
listKey: 'livestockList',
modalKey: 'livestockModal',
fields: [
{ name: 'householdCode', label: 'Mã hộ', required: true },
{ name: 'ownerName', label: 'Chủ hộ' },
{ name: 'animalType', label: 'Loại vật nuôi' },
{ name: 'quantity', label: 'Số lượng', type: 'number', defaultValue: 0 },
{ name: 'scale', label: 'Quy mô' },
{ name: 'status', label: 'Trạng thái' }
]
},
{
moduleKey: 'houses',
formKey: 'housesForm',
listKey: 'housesList',
modalKey: 'houseModal',
fields: [
{ name: 'householdCode', label: 'Mã hộ', required: true },
{ name: 'ownerName', label: 'Chủ hộ' },
{ name: 'houseType', label: 'Loại nhà' },
{ name: 'area', label: 'Diện tích', type: 'number', defaultValue: 0 },
{ name: 'address', label: 'Địa chỉ' },
{ name: 'status', label: 'Trạng thái' }
]
},
{
moduleKey: 'vehicles',
formKey: 'vehiclesForm',
listKey: 'vehiclesList',
modalKey: 'vehicleModal',
fields: [
{ name: 'plateNumber', label: 'Biển số', required: true },
{ name: 'ownerName', label: 'Chủ sở hữu' },
{ name: 'vehicleType', label: 'Loại xe' },
{ name: 'brand', label: 'Nhãn hiệu' },
{ name: 'registrationStatus', label: 'Đăng ký' },
{ name: 'status', label: 'Trạng thái' }
]
},
{
moduleKey: 'agriculture',
formKey: 'agricultureForm',
listKey: 'agricultureList',
modalKey: 'agricultureModal',
fields: [
{ name: 'householdCode', label: 'Mã hộ', required: true },
{ name: 'ownerName', label: 'Chủ hộ' },
{ name: 'cropType', label: 'Loại cây trồng' },
{ name: 'area', label: 'Diện tích', type: 'number', defaultValue: 0 },
{ name: 'season', label: 'Mùa vụ' },
{ name: 'status', label: 'Trạng thái' }
]
},
{
moduleKey: 'contributions',
formKey: 'contributionsForm',
listKey: 'contributionsList',
modalKey: 'contributionModal',
fields: [
{ name: 'householdCode', label: 'Mã hộ', required: true },
{ name: 'payerName', label: 'Người đóng' },
{ name: 'contributionType', label: 'Khoản đóng' },
{ name: 'amount', label: 'Số tiền', type: 'number', defaultValue: 0 },
{ name: 'period', label: 'Kỳ đóng' },
{ name: 'status', label: 'Trạng thái' }
]
}
].forEach(function (record) {
forms.register({
key: record.formKey,
moduleKey: record.moduleKey,
modalKey: record.modalKey,
sections: {
basic: record.fields.slice(0, 4),
linked: record.fields.slice(4, 5),
extended: record.fields.slice(5)
},
actions: [
{ key: 'save', label: 'Lưu', variant: 'success' },
{ key: 'cancel', label: 'Hủy', variant: 'light' }
]
});
lists.register({
key: record.listKey,
moduleKey: record.moduleKey,
screenId: record.moduleKey,
columns: record.fields.map(function (field) {
return { key: field.name, label: field.label, sortable: field.required === true || field.name === 'status' };
}),
filters: [
{ key: 'status', label: 'Trạng thái', type: 'select', defaultValue: '' }
],
rowActions: ['detail', 'edit'],
bulkActions: ['export']
});
crud.register({
moduleKey: record.moduleKey,
formKey: record.formKey,
detailFormKey: record.formKey,
listKey: record.listKey,
rowActions: ['detail', 'edit'],
bulkActions: ['export']
});
});
[
{ key: 'dashboard', label: 'Dashboard', icon: 'fa-gauge-high', items: ['dashboard', 'dashboardHouseholds', 'dashboardPopulation', 'dashboardBusiness', 'dashboardVehicles', 'dashboardLivestock', 'dashboardGis', 'dashboardReports'] },
{ key: 'overview', label: 'Tổng quan', icon: 'fa-tower-broadcast', items: ['operationCenter'] },
{ key: 'population', label: 'Quản lý dân cư', icon: 'fa-users', items: ['households', 'persons', 'temporaryResidence', 'temporaryAbsence', 'movements'] },
{ key: 'assets', label: 'Quản lý tài sản', icon: 'fa-building-columns', items: ['publicAssets', 'houses'] },
{ key: 'production', label: 'Quản lý sản xuất', icon: 'fa-seedling', items: ['businessHouseholds', 'agriculture', 'livestock'] },
{ key: 'vehicles', label: 'Quản lý phương tiện', icon: 'fa-car', items: ['vehicles'] },
{ key: 'finance', label: 'Quản lý tài chính', icon: 'fa-hand-holding-dollar', items: ['contributions'] },
{ key: 'gis', label: 'GIS', icon: 'fa-map-location-dot', items: ['gis'] },
{ key: 'reports', label: 'Báo cáo', icon: 'fa-chart-pie', items: ['reports'] },
{ key: 'data', label: 'Dữ liệu', icon: 'fa-file-import', items: ['import', 'exportExcel', 'printForms'] },
{ key: 'system', label: 'Hệ thống', icon: 'fa-user-shield', items: ['systemAdmin', 'users', 'permissions', 'logs', 'backups', 'restore', 'settings', 'appearance'] }
].forEach(function (menu) {
menus.register(menu);
});
var requiredBusinessModules = [
'households',
'persons',
'temporaryResidence',
'temporaryAbsence',
'movements',
'publicAssets',
'houses',
'businessHouseholds',
'agriculture',
'livestock',
'vehicles',
'contributions'
];
var dashboardModules = [
'dashboard',
'dashboardHouseholds',
'dashboardPopulation',
'dashboardBusiness',
'dashboardVehicles',
'dashboardLivestock',
'dashboardGis',
'dashboardReports'
];
[
{ key: 'dashboardModules', label: 'Dashboard modules', moduleKeys: dashboardModules },
{ key: 'migrationDashboard', label: 'Dashboard migration modules', moduleKeys: dashboardModules },
{ key: 'requiredBusinessModules', label: 'Required business modules', moduleKeys: requiredBusinessModules },
{ key: 'desktopModules', label: 'Desktop navigation modules', moduleKeys: requiredBusinessModules },
{ key: 'tabletModules', label: 'Tablet navigation modules', moduleKeys: requiredBusinessModules },
{ key: 'mobileModules', label: 'Mobile navigation modules', moduleKeys: requiredBusinessModules },
{ key: 'navigationRollout', label: 'Navigation rollout modules', moduleKeys: requiredBusinessModules }
].forEach(function (scope) {
navigationScopes.register(scope);
});
}
registerDefaults();
function installAppBridge() {
var currentApp = window.App;
try {
Object.defineProperty(window, 'App', {
configurable: true,
enumerable: true,
get: function () {
return currentApp;
},
set: function (value) {
currentApp = value;
modals.attachApp(currentApp);
}
});
} catch (_) {
return;
}
if (currentApp) modals.attachApp(currentApp);
}
installAppBridge();
if (document && typeof document.createElement === 'function' && document.body) {
menuRenderer.renderAll();
modals.registerBootstrapAll(document);
actions.bind(document);
}
window.Thon09Platform = platform;
if (document && document.dispatchEvent) {
document.dispatchEvent(new CustomEvent('thon09:platform-ready', { detail: { version: platform.version } }));
}
})(window, document);
